---
goal: Multi-Weight Font Variants for Fantasque Sans Mono
version: 1.11
date_created: 2026-07-31
last_updated: 2026-08-01
owner: Fantasque Sans Mono Core Team
status: 'Planned'
tags: [feature, architecture, interpolation, type-design]
upstream_prd: docs/prd-20260731-1000-multi-weight-variants.md
upstream_spec: spec/spec-multi-weight-variants.md
audit_reference: docs/audit/consistency-audit-plan-vs-prd-spec-2026-08-01.md
clarification_reference: docs/audit/clarification-report-implementation-plan-multi-weight-variants-2026-08-01-r5.md
---
<!-- markdownlint-disable -->

# Introduction

![Status: Planned](https://img.shields.io/badge/status-Planned-lightgrey)

This plan outlines the step-by-step execution to implement Multi-Weight Font Variants (Medium, SemiBold, Light, ExtraBold) for Fantasque Sans Mono via FontForge master harmonization and linear interpolation. The execution strictly preserves legacy source files (CON-001) and integrates with the existing Custom Build Workflow. Stretch weights (Light 300, ExtraBold 800) only ship via the release upstream pipeline after core weights pass visual review (E10) — Custom Build produces core weights only.
Sesi klarifikasi kedua (laporan r2, 2026-07-31) menghasilkan 12 resolusi yang diimplementasikan di v1.7 — termasuk: definisi "release upstream pipeline" (eksekusi manual maintainer di luar CI), penguncian factor SemiBold 0.67 (toleransi test ±0.005), kalibrasi inheren threshold 15.0°, gate PoC ganda (script + manusia), verifikasi metadata dua lapis, mode switch packaging (Custom Build tetap 1 archive), daftar pesan log aktual GUD-003/AC-B05, dan fixture dummy input sintetis.
Sesi klarifikasi ketiga (laporan r3, 2026-08-01) menghasilkan 16 resolusi (K1–K16) yang diimplementasikan di v1.9 — termasuk: koreksi RUN chain final Dockerfile (guard kondisional dengan grep portabel tanpa here-string `<<<` (Stage 1 pakai `/bin/sh`/dash tanpa `SHELL` directive), stripping `--multi-weight` via `DRIVER_ARGS` sebelum pemanggilan driver agar tidak ditolak `parse_args()` — CON-001 tidak tersentuh; instalasi `pytest` + `jsonschema` tanpa kondisi di Stage 1; validasi `--strict` untuk KEDUA pasangan master; loop fail-fast `validate_interpolation.py --threshold T_final --fail-fast` per core weight — GUD-002 ter-enforce di CI), gate harmonisasi dua-tingkat (`pass_rate ≥ 98%` = checkpoint kemajuan; `fail_count = 0` = syarat masuk Phase 3), pembagian 70/25/5 sebagai label porsi beban (bukan batas cakupan — setiap designer mengharmonisasi 100% pasangan yang ditugaskan), parameter eksplisit `--threshold DEG` + protokol dua-pass kalibrasi threshold (gate dievaluasi pada laporan threshold final R2), output ganda `poc_interpolation.py` (`.sfdir` + TTF tanpa hinting agar kompatibel specimen HTML), mekanisme pohon sintetis test assembly (TASK-0.10), TASK-0.13 baru + FILE-023 (`tests/test_validate_interpolation.py`, 5 test case Spec §6.3), eksekusi pytest Phase 0 di container Stage 1 (FontForge nyata) + `importorskip` di keempat file test, override hinting REQ-I04 (weight baru selalu di-hint terlepas `UseHinted`), definisi eksplisit komposisi 6 weight budget WOFF2 ≤ 500 KB, definisi `RELEASE_MODE` (env var eksplisit yang di-invoke maintainer; CI Custom Build tidak pernah menyetelnya), runbook release upstream 9 langkah (TASK-5.4), entri `.gitignore` (`build/`, `Sources/Harmonized/Interpolated/`), dan penyelarasan jumlah `.sfdir` hasil assembly (7 Custom Build / 9 release upstream).

Sesi klarifikasi keempat (laporan r4, 2026-08-01) menghasilkan 5 resolusi (R1–R5) + 1 minor observation (MO-1) yang diimplementasikan di v1.10 — termasuk: penghapusan total cabang `ENABLE_MULTI_WEIGHT` di `packaging.sh` (perilaku Custom Build kembali ke zip-all existing — otomatis menyertakan Medium/SemiBold/FantasqueSans; override hinting REQ-I04 diputuskan murni dari pola nama file `NEW_WEIGHTS`; satu-satunya mode eksplisit tetaplah `RELEASE_MODE=1`, R1), env var eksplisit `VERSION` (wajib pada mode release — guard `_die` jika kosong — untuk penamaan archive `FantasqueSansMono-{VERSION}-{Format}.zip`, R2), penguncian factor SemiBold eksak `0.67` (toleransi test ±0.005 dimaknai kelonggaran presisi float, bukan kebebasan memilih nilai, R3), TASK-0.14 baru + FILE-025 (workflow push-gate `.github/workflows/test-multi-weight.yml` — memenuhi kewajiban Spec §6.6, R4), dan guard eksistensi `Sources/Harmonized/{Regular,Bold,Italic,BoldItalic}` di awal RUN chain Stage 1 dengan pesan `::error::` + exit 1 (R5).
Sesi klarifikasi kelima (laporan r5, 2026-08-01) menghasilkan 20+ resolusi yang dikelompokkan menjadi 8 kategori: **B1 (kritis)** — instalasi `python3-fontforge` via `apt` (bukan pip) di Stage 1 container agar keempat file test FontForge-dependent benar-benar dieksekusi penuh, bukan hanya `importorskip`-skip; pre-check `python3 -c "import fontforge"` di awal RUN chain; **MO-1** — `pytest-cov` coverage ≥ 90% di container (host runner tidak mengukur, hanya smoke gate); **B2** — variabel shell `T_FINAL` (dari `phase0-experiments-{date}.md` + `visual-quality-rubric.md`) di RUN chain validasi, bukan hardcode `15.0`; **B3** — langkah lokal TTF pra-hinting via `custom_build_driver.py build/sources <tmp>` sebelum FontForge post-processing; **H1** — generalisasi `T_final` (R-full `phase0-experiments` vs PoC fallback `15.0`); **H2** — FILE-024 owned-by TASK-0.7 (Files column eksplisit: `2 (FILE-006 + FILE-024)`); **H3** — surfacing laporan JSON ke artifact build (`mkdir -p build` Stage 1 → `COPY --from=builder-fontforge /build/build /app/build-reports` Stage 2 → `packaging.sh` copy ke `output/reports/` → workflow upload `output/reports/**`); **H4** — push-gate = smoke gate, bukan coverage gate; **H6** — runbook TASK-5.4 dianotasi per stage. **E1** — aturan kolaborasi `tracking.json` (no-reformat, union + sort, E1); **E2** — validasi stretch per weight terpisah + jalur eksklusi (`--exclude WEIGHT` flag, GUD-004); **E4 dilewati** — spec amendment (§4.4 exit code contract), tidak ada plan change; **E5** — hmtx copy → unconditional; **E7** — REQ-I06 "dikeluarkan SEMENTARA" di tracking.json; **E8** — AC-B02 parenthetical.

**Koordinasi cross-document (amendemen upstream r4 — referensi r4 §4):** amendemen `spec/spec-multi-weight-variants.md` (→ v1.6): §4.10 (pseudo-code packaging disederhanakan — hapus cabang `ENABLE_MULTI_WEIGHT`; perilaku berbasis file zip-all + pola nama `NEW_WEIGHTS`; definisi `VERSION` env var eksplisit, wajib pada `RELEASE_MODE=1`, guard `_die`; penamaan archive `FantasqueSansMono-${VERSION}-${Format}.zip` — R1/R2), §4.9 (guard eksistensi `Sources/Harmonized/{Regular,Bold,Italic,BoldItalic}` dengan pesan `::error::` + exit 1 — R5), REQ-I01/§6.3 test case 2 (nilai eksak `0.67` dikunci; toleransi ±0.005 = kelonggaran presisi float — R3), §4.6/§6.6 (catatan implementasi push-gate workflow `test-multi-weight.yml`, host runner, `importorskip` — R4) — **DIJADWALKAN** via `/sdlc-define-specs` di sesi terpisah (belum diterapkan di Spec v1.5 saat dokumen ini ditulis). PRD: tidak ada amendemen wajib — catatan interpretasi FR-4.1 ("~0.67" → eksak `0.67`) bersifat opsional (R3). `CONTEXT.md`: tidak ada istilah kanonis baru (`VERSION` dan `RELEASE_MODE` adalah variabel teknis, bukan istilah domain) — tidak ada pembaruan glossary. ADR: tidak ada keputusan yang memenuhi triple-gate — seluruh resolusi (R1–R5) bersifat mudah dibalik dan masuk kategori spec-level resolutions (Spec §8.5).

**Koordinasi cross-document (amendemen upstream — referensi r2 §4 + audit konsistensi 2026-08-01, temuan B7):** daftar amendemen diperluas meliputi Spec §4.7 (C1 — `features.py` in-process per `.sfdir` di build/sources) & §4.9 (B7 — RUN chain + `RUN pytest tests/` fail-fast di Stage 1) serta PRD FR-7.2 (B2), GH-004 AC#3 (B3), GH-005 AC#2 (B4), FR-7.1 (B5), §9.2 Phase 3 (B6), dan path `scripts/` → `Scripts/` (D3). Amendemen `spec/spec-multi-weight-variants.md` (REQ-I02 — factor stretch ditetapkan sebelum produksi stretch, Phase 5 —, REQ-H06/§11.2 — kalibrasi threshold 15.0° —, §4.6 — injeksi metadata os2_weight/fullname —, §4.7, §4.9, §4.11 — definisi operasional `warning`/`fail` —, §4.12 — schema `tracking.json` + field `review_verdict`/`reviewed_by`/`date` —, §6.2 — path rubric kanonik `docs/audit/visual-quality-rubric.md` + catatan E0.2 —, §6.3 — 11 test case termasuk `test_metadata_injection` —, §6.5 — konten fixture dikunci —, GUD-003/AC-B05 — daftar pesan log aktual —, §8.5 — definisi Release Upstream Pipeline —) **SELESAI diterapkan di Spec v1.4** (2026-08-01). Amendemen `docs/prd-20260731-1000-multi-weight-variants.md` (FR-2.4 gate ganda — A10 —, FR-7.1 RUN kondisional Stage 1 — B5 —, FR-7.2 byte-identical — B2 —, GH-004 AC#3 5 file output — B3 —, GH-005 AC#2 specimen HTML — B4 —) **SELESAI diterapkan di PRD v1.4**; sisa amendemen PRD (GH-004 AC#5 daftar pesan log — A3 —, §8.1 + E0.1 model pemanggilan `features.py` — A11 —, §9.2 Phase 3 penjadwalan stretch — B6 —, path `scripts/` → `Scripts/` — D3 —) masih dijadwalkan via `/sdlc-draft-prd`. Istilah "Release Upstream Pipeline" telah ditambahkan ke `CONTEXT.md` (sesi klarifikasi r2).
**Koordinasi cross-document (amendemen upstream r3 — referensi r3 §4):** amendemen `spec/spec-multi-weight-variants.md` (→ v1.5): §4.9 (koreksi RUN chain — grep portabel tanpa `<<<`, stripping `--multi-weight` → `$DRIVER_ARGS`, validasi harmonisasi kedua pasangan, loop `validate_interpolation.py --fail-fast` per core weight, instalasi pytest/jsonschema Stage 1), §4.6 (kontrak driver — gating distorsi berat via `validate_interpolation.py` bukan deteksi internal driver; output TTF `poc_interpolation.py`), §4.10 (definisi `RELEASE_MODE`; definisi komposisi 6 weight budget WOFF2 AC-D03), §6.3 (catatan `importorskip` untuk seluruh file test FontForge-dependent; kontrak parameter `--threshold`), §6.5 (mekanisme test assembly pohon sintetis temp), §7 (item *Ask First* "Dockerfile package" ditandai disetujui untuk pytest/jsonschema pada konteks r3), §9.2 (SVC-005 diperluas: future, pytest, jsonschema), §11.1 (VAL-017 — 7 `.sfdir` Custom Build / 9 release upstream), AC-H02 (catatan gate dua-tingkat), REQ-H06/§4.11 (parameter `--threshold` + protokol dua-pass kalibrasi), REQ-I04 (mekanisme override UseHinted di packaging — weight baru selalu di-hint) — **DIJADWALKAN** via `/sdlc-define-specs` di sesi terpisah (belum diterapkan di Spec v1.4 saat dokumen ini ditulis). PRD: tidak ada amendemen wajib — catatan interpretasi SM-T4 (komposisi 6 weight budget WOFF2, K10) bersifat opsional. `CONTEXT.md`: tidak ada istilah kanonis baru (`RELEASE_MODE` adalah variabel teknis, bukan istilah domain). ADR: tidak ada keputusan yang memenuhi triple-gate (hard to reverse / surprising / real trade-off) — seluruh resolusi bersifat mudah dibalik, masuk kategori spec-level resolutions.
**Koordinasi cross-document (amendemen upstream r5 — referensi r5 §4 Next Steps):** amendemen `spec/spec-multi-weight-variants.md` (→ v1.7, terjadwalkan via `/sdlc-define-specs`) meliputi: §4.4 (exit code contract — `validate_interpolation.py` keluar non-zero pada `fail`, keluar 0 pada `warning`; kode keluar 2 untuk usage error — **E4 dilewati**, tidak ada plan change); §4.6 (hmtx copy → *unconditional*, E5 — tidak lagi bersyarat pada flag `--include-stretch`); §4.8 (`--light-factor`/`--extrabold-factor` menjadi **wajib bersama `--enable-*`** tanpa default di kode — E6); §4.9 (loop validasi threshold = `${T_FINAL}` shell variable, **B2** — bukan hardcode `15.0`; sumber: `phase0-experiments-{date}.md` + `visual-quality-rubric.md`); §4.12 (aturan kolaborasi `tracking.json` — no-reformat, union + sort, **E1**). REQ-I03 (generalisasi `T_final` sumber — H1), REQ-I06 ("dikeluarkan SEMENTARA" di tracking.json — E7), AC-B02 (parenthetical — E8), §6.7 (`pytest-cov` coverage ≥ 90% di container — MO-1). **E4 dilewati**: Spec §4.4 exit code contract — tidak ada modifikasi TASK-0.1 yang diperlukan. Perubahan ini adalah **spec amendment**, bukan plan change.

## 1. Requirements & Constraints

- **REQ-H01 to H06**: Master Harmonization & Visual Invariants
- **REQ-I01 to I07**: Multi-Weight Interpolation, Auto-Hinting, and Metric Preservation
- **REQ-B01 to B06**: Build Pipeline Integration (Pre-processing in Stage 1)
- **REQ-D01 to D04**: Output Formats, File Naming, and Release Packaging
- **REQ-S01 to S05**: Specimen Sheet & Visual QA Validation
- **CON-001**: Legacy Code Preservation (build.py, fontbuilder.py, features.py MUST NOT be modified)
- **CON-002**: Workflow A — FontForge Interpolation Only
- **CON-003**: Linear Interpolation Only — hanya interpolasi linear via `font.interpolateFonts()`; tidak ada optical correction
- **CON-004**: No Stroke Modification — `font.changeWeight()` atau algoritma stroke modification otomatis TIDAK BOLEH digunakan — hanya interpolasi antar master yang diharmonisasikan
- **CON-005**: Vertical Metrics Freeze (Ascent, Descent, LineGap MUST NOT change)
- **CON-006**: No Italic instances generated for new weights (Faux Italic fallback)
- **CON-007**: GitHub Actions Free-Tier — build HARUS berjalan pada runner free-tier (`ubuntu-latest`) dengan batas waktu 360 menit
- **CON-008**: FontForge Python 3 Bindings — seluruh script HARUS kompatibel dengan bindings default Ubuntu 26.04 + `future` shim
- **GUD-001**: Deterministic Build — dua run dengan input identik HARUS menghasilkan file font byte-identical
- **GUD-002**: Fail-Fast on Critical Error — kegagalan interpolasi dengan distorsi kontur berat (self-intersection, counter tertutup) HARUS menghentikan pipeline dengan exit code non-zero dan pesan diagnostik. *Enforcement (r3 K8): via loop `validate_interpolation.py --threshold T_final --fail-fast` di RUN chain (TASK-4.2) — driver TIDAK menduplikasi logika deteksi (cukup mempropagasi error FontForge; lihat TASK-0.7).*
- **GUD-003**: Progress Logging — Build log MUST display progress per stage
- **GUD-004**: Partial Success Tolerance — Stretch weight failure does NOT fail entire build
- **SEC-001**: No User Data — tidak ada data pengguna yang dikumpulkan, disimpan, atau ditransmisikan
- **SEC-002**: Scoped Token — token GitHub Actions hanya memiliki izin `contents: write` yang terbatas pada fork tempat workflow dijalankan

## 2. Implementation Steps

> **EXECUTION DIRECTIVE FOR AI AGENTS:**
> You MUST execute this plan phase by phase. You MUST run the specific testing/verification task at the end of each phase. After a phase is tested, you **MUST STOP AND WAIT** for the user's explicit approval before proceeding to the next phase.

### Implementation Phase 0: Tooling & PoC Preparation

- GOAL-001: Membangun fondasi *script* otomatis (deteksi, validasi, spesimen, interpolasi), menyusun standar kualitas visual, dan menyiapkan infrastruktur pengujian sebelum pekerjaan manual dimulai.

| Task      | Description                                                                  | Ref ID  | AC Ref | Dep   | Files | Completed | Date |
| --------- | ---------------------------------------------------------------------------- | ------- | ------ | ----- | ----- | --------- | ---- |
| TASK-0.0  | Buat *branch* Git terisolasi `feature/multi-weight-poc`                      | -       | -      | -     | -     |           |      |
| TASK-0.1  | Buat `Scripts/detect_incompatibility.py` (Deteksi kontur)                    | REQ-H01 | AC-H01 | -     | 1     |           |      |
| TASK-0.2  | Buat `Scripts/validate_harmonization.py` (Validasi node & kontur, termasuk implementasi deteksi *tangent-angle*) | REQ-H02 | -      | -     | 1     |           |      |
| TASK-0.3  | Buat `Scripts/validate_interpolation.py` (Validasi hasil interpolasi, termasuk deteksi *tangent-angle*). Definisi operasional unit = per glyph: `warning` = artifact non-self-intersect (tangent-angle > threshold); `fail` = self-intersection / counter tertutup / kontur rusak. **Parameter eksplisit `--threshold DEG` (default 15.0) + flag `--fail-fast`** (r3 K7/K8) — nilai threshold TIDAK di-parse dari rubric markdown (rubric hanya mendokumentasikan nilai final, bukan sumber konfigurasi script); `--fail-fast` → exit non-zero pada `fail` pertama. | REQ-S05 | AC-I08 | -     | 1     |           |      |
| TASK-0.4  | Buat `Scripts/generate_specimen.py` (Generator HTML *waterfall*)             | REQ-S01 | AC-S01 | -     | 1     |           |      |
| TASK-0.5  | Buat `docs/audit/visual-quality-rubric.md` (PATH KANONIK — keputusan plan v1.7: lokasi `docs/audit/`; Spec §6.2 E0.4 diamendemen mengikuti, lihat koordinasi cross-document) & `Sources/Harmonized/tracking.json` (schema diperluas: field `review_verdict` (`pass\|fail`), `reviewed_by`, `date` — Spec §4.12 diamendemen) | -       | AC-P03 | -     | 2     |           |      |
| TASK-0.6  | Buat `Scripts/poc_interpolation.py` (Script interpolasi subset) — **output ganda (r3 K11): `.sfdir` interpolated subset DAN TTF untuk rendering via `font.generate` (TANPA hinting — konsisten "TANPA ttfautohint")**. TTF menjadi input rendering ke TASK-1.2/1.3 (specimen HTML & visual diff review FR-2.3 yang butuh font renderable pada 8/12/16/24 pt). | REQ-I01 | AC-P02 | -     | 1     |           |      |
| TASK-0.7  | Buat `Scripts/multi_weight_driver.py` (Interpolasi & Assembly): **factor interpolasi dikunci sebagai kontrak spesifikasi — Medium = 0.5, SemiBold = `0.67` eksak (dua desimal; r4 R3 — "~0.67" pada dokumen upstream adalah pembulatan presentasi; toleransi test ±0.005 hanyalah kelonggaran presisi float, BUKAN kebebasan memilih nilai, karena `0.67` vs `2/3` = 0.6666… menghasilkan koordinat interpolasi berbeda hingga 0.34% dari ekstrem master dan melanggar determinisme GUD-001 antar-varian implementasi)**, flag `--enable-light`/`--enable-extrabold` (HANYA dipakai release upstream, E10 — bukan `--include-stretch`), TIDAK memanggil `features.py` & TIDAK menjalankan `ttfautohint` (Spec §4.6), injeksi metadata internal (os2_weight, fullname) sebelum menyimpan `.sfdir`, copy-as-fallback + hmtx copy, assembly `build/sources/` (REQ-B06). **Kontrak driver (r3 K8): driver cukup mempropagasi error FontForge — gating distorsi berat adalah tanggung jawab `validate_interpolation.py` (satu sumber deteksi, tanpa duplikasi logika). Jumlah `.sfdir` hasil assembly (r3 K16): 7 pada mode Custom Build (4 harmonized masters + Medium + SemiBold + `FantasqueSans.sfdir`) dan 9 pada release upstream dengan stretch (8 weight + `FantasqueSans.sfdir`) — selaras Spec §4.7 (7–9). Tambahan r3 K15: **entri `.gitignore`: `build/` dan `Sources/Harmonized/Interpolated/`.** | REQ-B06 | AC-B02 | -     | 2     |           |      |
| NOTE-0.7| **ADD (r5 H2)**: FILES column diupdate eksplisit — `2 (FILE-006 + FILE-024)` (bukan angka "2" saja). FILE-006 (`Scripts/multi_weight_driver.py`) dan FILE-024 (`.gitignore`) dikelola sebagai satu unit oleh TASK-0.7; FILE-024 bertanggung jawab mengeskusi `build/` & `Sources/Harmonized/Interpolated/` untuk VCS-ignore (r3 K15 + r5 H2). | REQ-I01 | AC-I01 | TASK-0.1 | 2 | | |
| TASK-0.8  | Buat `tests/test_detect_incompatibility.py` (Unit test deteksi kontur)       | Spec §6.3 | -   | TASK-0.1 | 1  |           |      |
| TASK-0.9  | Buat `tests/test_validate_harmonization.py` (Unit test validasi harmonisasi) | Spec §6.3 | -   | TASK-0.2 | 1  |           |      |
| TASK-0.10 | Buat `tests/test_multi_weight_driver.py` — 11 test case Spec §6.3. Tambahan: toleransi test SemiBold factor ±0.005, test case ke-11 `test_metadata_injection`. Menggunakan `pytest.importorskip("fontforge")`. **Interpretasi r4 R3 pada test case 2 (`test_semibold_interpolation_factor`): memverifikasi nilai eksak `0.67` (bukan "~0.67"); toleransi ±0.005 dimaknai kelonggaran presisi float (artefak perhitungan titik mengambang), BUKAN kebebasan memilih nilai — dua implementer yang memilih `0.67` vs `2/3` menghasilkan output berbeda dan melanggar semangat determinisme GUD-001.** *Mekanisme test assembly (r3 K12): test case #9/#10 membangun pohon sintetis lengkap di direktori temp — menyalin fixture ke `Harmonized/{Regular,Bold}`, menambah stub `Harmonized/{Italic,BoldItalic}` (salinan minimal master) dan stub `FantasqueSans.sfdir` (minimal) via helper test, BUKAN fixture yang di-commit; konten fixture yang di-commit tidak berubah.* Deliverable: fixture `.sfdir` (konten dikunci: 2 master, ≥6 glyph termasuk advance width beda & edge cases) di `tests/fixtures/multi_weight/`. | Spec §6.3 | -   | TASK-0.7 | 1  |           |      |
| TASK-0.11 | Jalankan eksperimen E0.1 (Idempotensi) & E0.2 (Advance Width). *Catatan: E0.2 adalah eksperimen terpisah, subset 10 glyph-nya tidak wajib identik dengan FR-2.1.* | REQ-I03 | AC-P05 | -     | -     |           |      |
| TASK-0.12 | Jalankan eksperimen E0.3 (Simulasi paralel 2-designer)                       | -       | AC-P01 | -     | -     |           |      |
| TASK-0.13 | **BARU** — Buat `tests/test_validate_interpolation.py`: 5 test case Spec §6.3 (pass, warning, fail, overlay PNG, JSON valid; validasi struktur laporan + `--fail-fast` exit code). Menggunakan `pytest.importorskip("fontforge")` di level modul (r3 K5/K6). | Spec §6.3 | -   | TASK-0.3 | 1  |           |      |
| TASK-0.14 | **BARU** — Buat `.github/workflows/test-multi-weight.yml` (r4 R4): trigger `push` ke branch `feature/multi-weight-*`; step: `pip install pytest jsonschema` + `pytest tests/ -v` di **host runner** (tanpa Docker). Keempat file test FontForge-dependent di-skip otomatis via `pytest.importorskip("fontforge")` di level modul (r3 K6); eksekusi nyata dengan FontForge tetap terjadi di container Stage 1 (RUN chain TASK-4.2 / TASK-0.X). Biaya rendah (tanpa build Docker per push) — **memenuhi kewajiban Spec §6.6 (unit test dijalankan pada setiap push ke branch `feature/multi-weight-*`; workflow `custom-build.yml` existing bersifat `workflow_dispatch` manual sehingga tidak memenuhi trigger push)**; workflow e2e `test-multi-weight-build.yml` (§6.4) tetap opsional — verifikasi e2e dijalankan manual via TASK-4.X. | Spec §6.6 | -   | TASK-0.13 | 1  |           |      |
| TASK-0.X  | **VERIFY**: Jalankan seluruh unit test (`pytest`) DI DALAM image `builder-fontforge` (container Stage 1 — FontForge nyata) via `docker run` ad-hoc: `pip3 install --break-system-packages pytest jsonschema` + bind-mount repo (sebelum TASK-4.2 membakukan instalasi di Dockerfile resmi; **bukan** di host runner — r3 K6). Keempat file test yang menyentuh FontForge (`test_detect_incompatibility.py`, `test_validate_harmonization.py`, `test_validate_interpolation.py`, `test_multi_weight_driver.py`) memakai `pytest.importorskip("fontforge")` di level modul agar gate host runner tidak crash. **Push-gate host runner (Spec §6.6, r4 R4) dijalankan via workflow `test-multi-weight.yml` (TASK-0.14) — setiap push ke `feature/multi-weight-*`; peran: smoke gate cepat (skipped tests), eksekusi nyata tetap di sini/Stage 1.** Jalankan juga semua script validasi (`detect_incompatibility.py`, `validate_harmonization.py`, `validate_interpolation.py`) dengan dummy input (menggunakan fixture dari `tests/fixtures/multi_weight/`) memastikan tidak ada *syntax error* dan test *passing*. | - | - | - | - | | |
| NOTE-0.X| **ADD (r5 B1, MO-1)**: (i) **Instal `python3-fontforge` via `apt` di container Stage 1** — `RUN apt-get update && apt-get install -y --no-install-recommends python3-fontforge && rm -rf /var/lib/apt/lists/*` — agar keempat file test FontForge-dependent benar-benar dieksekusi penuh, bukan hanya `importorskip`-skip. Pre-check: `python3 -c "import fontforge"` di awal RUN chain (fail-fast jika modul tidak tersedia, memberi error informatif bagi fork owner). (ii) **`pytest-cov` dijalankan di dalam container Stage 1** untuk coverage measurement — `pytest --cov=Scripts --cov-report=term-missing`. **Host runner (TASK-0.14/TASK-0.X) TIDAK mengukur coverage** (MO-1: coverage gate = container-only; push-gate = smoke gate H4). | - | - | - | 1 | | |
| TASK-0.Y  | **APPROVAL**: Wait for explicit user confirmation to proceed to Phase 1      | -       | -      | -     | -     |           |      |

### Implementation Phase 1: Proof of Concept (MVP)

- GOAL-002: Memvalidasi kelayakan harmonisasi dan interpolasi pada subset terbatas (~40-50 *glyph* kritis) sebelum eksekusi massal.

| Task     | Description                                                     | Ref ID  | AC Ref | Dep      | Files | Completed | Date |
| -------- | --------------------------------------------------------------- | ------- | ------ | -------- | ----- | --------- | ---- |
| TASK-1.1 | Harmonisasi manual ~40-50 *glyph* untuk pasangan Regular ↔ Bold berdasarkan kriteria *worst offenders* kuantitatif (node_diff, contour mismatch, prioritas fungsional). Daftar didokumentasikan di artefak audit `docs/audit/poc-glyph-list-{date}.md`. | REQ-H03 | AC-P01 | TASK-0.2 | 1-2   |           |      |
| TASK-1.2 | Interpolasi subset ke Medium (500) menggunakan `poc_interpolation.py` — TANPA `ttfautohint` (hinting hanya di Stage 2, Spec §8.5; perbandingan visual adil pada level rendering yang sama: kedua font belum di-*hint*). **Memakai output TTF dari TASK-0.6 (r3 K11).** | REQ-I01 | AC-P02 | TASK-1.1 | -     |           |      |
| TASK-1.3 | Hasilkan Specimen Sheet HTML untuk hasil interpolasi Medium. Lakukan *visual diff review* sekaligus kalibrasi inheren threshold 15.0° untuk mendeteksi *tangent-angle* (hasil dicatat di rubric dan laporan E0). **Protokol dua-pass kalibrasi (r3 K7): (1) jalankan `validate_interpolation.py --threshold 15.0` → laporan R1; (2) *visual diff review* (FR-2.3) mengkalibrasi threshold → nilai final `T_final` dicatat di `docs/audit/phase0-experiments-{date}.md` + `visual-quality-rubric.md`; (3) jalankan ulang script dengan `--threshold T_final` → laporan R2; (4) gate TASK-1.X dievaluasi pada laporan R2. Guard: perubahan threshold WAJIB didasari temuan visual dan didokumentasikan — tidak boleeh disetel hanya agar gate lolos.** | REQ-S02 | AC-P06 | TASK-1.2 | 1     |           |      |
| TASK-1.X | **VERIFY**: Gate ganda PoC — (1) Script: laporan **R2** `validate_interpolation.py` (threshold final `T_final`, protokol dua-pass r3 K7) menunjukkan `pass_rate >= 90%` dan `fail_count = 0`. (2) Manusia: **review mencakup 100% glyph PoC (~40–50 glyph; biaya trivial — r3 K13)**, setara dengan AC-P03 — ≥90% dari seluruh glyph yang direview mempertahankan nuansa *handwritten* + `fail_count = 0` (script). | -       | AC-P03 | -        | -     |           |      |
| TASK-1.Y | **APPROVAL**: Wait for explicit user confirmation (PoC Lulus) to proceed to Phase 2. Jika GAGAL, ikuti salah satu dari 4 jalur keputusan FR-2.5: (1) Iterasi harmonisasi ulang pada subset PoC — maksimal 2 siklus tambahan, (2) Revisi cakupan V1 — batasi hanya Medium+SemiBold tanpa *stretch weight*, (3) Re-evaluasi keputusan tooling — pertimbangkan percepatan migrasi ke Workflow B (UFO/`fontmake`), (4) Penundaan/penghentian fitur — jika ketiga opsi di atas tidak layak. Keputusan akhir WAJIB didokumentasikan sebagai catatan pada PRD (revisi minor) atau ADR baru. | - | AC-P07 | - | - | | |

### Implementation Phase 2: Full Master Harmonization (Core Experience)

- GOAL-003: Melakukan harmonisasi manual pada seluruh *glyph* Regular ↔ Bold dan Italic ↔ BoldItalic (pekerjaan utama *type designer*).

| Task      | Description                                                                                          | Ref ID  | AC Ref | Dep      | Files | Completed | Date |
| --------- | ---------------------------------------------------------------------------------------------------- | ------- | ------ | -------- | ----- | --------- | ---- |
| TASK-2.1  | Harmonisasi manual Regular ↔ Bold — Designer A (Lead): Latin/Cyrillic/Greek. **`~70%` adalah label informal porsi beban/prioritas, BUKAN batas cakupan (r3 K4): Designer A mengharmonisasi 100% glyph pasangan Regular↔Bold; AC-H02 berlaku penuh.** | REQ-H03 | AC-H04 | TASK-1.Y | 2     |           |      |
| TASK-2.2  | Harmonisasi manual Italic ↔ BoldItalic — Designer B (Support): Latin/Cyrillic/Greek. **`~25%` label porsi beban informal, BUKAN batas cakupan (r3 K4): Designer B mengharmonisasi 100% glyph pasangan Italic↔BoldItalic; AC-H02 berlaku penuh — hasil harmonisasinya ikut di-assembly ke `build/sources/` (TASK-0.7/3.1).** | REQ-H03 | AC-H05 | TASK-1.Y | 2     |           |      |
| TASK-2.3  | **Shared pool** (Symbols, ligatures, PUA, box-drawing — `~5%` label porsi beban informal, bukan batas cakupan; r3 K4): *first-come-first-served* via Git branch terisolasi, dikerjakan bersamaan dengan TASK-2.1 & 2.2. Jika *deadlock*, eskalasi ke *upstream maintainer*. | PRD §9.1 | - | TASK-1.Y | - | | |
| NOTE-2.3| **ADD (r5 E1)**: Aturan kolaborasi `tracking.json` (berlaku TASK-2.1/2.2/2.3 shared pool) — (1) Designer A & B **tidak merformat ulang atau mengubah urutan** array `glyphs` di luar glyph yang sedang mereka kerjakan (minimalkan konflik git); (2) konflik merge `tracking.json` diresolevkansi dengan **union + sort**: semua entry dipertahankan (tidak ada field di-drop), urutan akhir di-sort berdasarkan `glyph_name`; (3) duplicate `glyph_name + check_type` → overwrite dengan verdict terbaru; (4) aturan dicatat di Spec §4.12 (amendemen terjadwal). | PRD §9.1 | - | TASK-1.Y | - | | |
| TASK-2.4  | *Visual spot-check* per *glyph family* (misal: semua *arrow glyphs*, *bracket glyphs*, *currency glyphs*) secara berkala selama harmonisasi berlangsung | PRD §9.2 | - | TASK-1.Y | - | | |
| TASK-2.X  | **VERIFY**: **Gate dua-tingkat harmonisasi (r3 K3)** — `validate_harmonization.py` untuk **KEDUA** pasangan master (Regular↔Bold DAN Italic↔BoldItalic): *tingkat 1 — `pass_rate >= 98%` = checkpoint kemajuan* (melanjutkan review/iterasi; AC-H02 tetap bermakna sebagai metrik checkpoint/SM-T1); *tingkat 2 — `fail_count = 0` (100%) = syarat masuk Phase 3* (interpolasi). Pengerjaan sisa ≤2% adalah kelanjutan alami TASK-2.1/2.2 — **TIDAK ada task baru**. Konsisten dengan *Never Do* Spec §7 (seluruh glyph `fail` HARUS diperbaiki sebelum interpolasi), GUD-002, dan `--strict` di RUN chain (TASK-4.2). | -       | AC-H02 | -        | -     |           |      |
| TASK-2.Y  | **APPROVAL**: Wait for explicit user confirmation to proceed to Phase 3                              | -       | -      | -        | -     |           |      |

### Implementation Phase 3: Multi-Weight Interpolation & QA (Resilience)

- GOAL-004: Menghasilkan **core weights** (Medium 500, SemiBold 600), melakukan pasca-pemrosesan (hmtx & assembly), QA visual menyeluruh, dan *iterative fix loop*. *Stretch weights* (Light, ExtraBold) ditangguhkan ke *release upstream pipeline* (Spec §4.6/§8.5, E10).

| Task     | Description                                                                                          | Ref ID  | AC Ref | Dep      | Files | Completed | Date |
| -------- | ---------------------------------------------------------------------------------------------------- | ------- | ------ | -------- | ----- | --------- | ---- |
| TASK-3.1 | Eksekusi `multi_weight_driver.py` untuk **Core Weights only** (Medium 500 + SemiBold 600) — tanpa flag `--enable-light`/`--enable-extrabold` (stretch HANYA di release upstream, E10); **factor interpolasi: Medium = 0.5, SemiBold = `0.67` eksak (r4 R3 — kontrak spesifikasi; lihat TASK-0.7)**; driver meng-inject metadata internal (os2_weight, fullname) sebelum menyimpan `.sfdir`. **Input: 4 master terharmonisasi dari KEDUA pasangan (Regular↔Bold, Italic↔BoldItalic — r3 K4); assembly 7 `.sfdir` ke `build/sources/` (r3 K16).** | REQ-I01 | AC-I01 | TASK-2.Y | -     |           |      |
| TASK-3.2 | Hasilkan Full Specimen Sheet (HTML) untuk seluruh **core weight** — termasuk informasi metrik: *stem width*, *x-height*, *cap height*, *advance width* per weight. QA visual pra-*hinting* via specimen sheet HTML + FontForge rendering preview (TTX/text-mode) — cukup untuk mendeteksi distorsi kontur & *discontinuity* sebelum hinting (Spec §8.5). Reviewer: Designer A (Lead). Keputusan QA didokumentasikan di `tracking.json` dan lampiran ringkasan `docs/audit/phase3-visual-review-{date}.md`. | REQ-S02 | AC-S02, AC-S04 | TASK-3.1 | - |           |      |
| NOTE-3.2 | **ADD (r5 B3)**: Sebelum FontForge post-processing (TASK-3.Y → TASK-4.2), tambahkan **lokal TTF generation step** — `custom_build_driver.py build/sources <tmp_dir>` untuk menghasilkan TTF pra-hinting (Medium, SemiBold) di `build/pre_hint/`. TTF ini menjadi input ke FontForge `multi_weight_driver.py` untuk post-processing (hinting via `ttfautohint` di Stage 2, penempatan nama, OS/2). Output: `build/pre_hint/{weight}.ttf` — diekspor sebelum `fontforge -script` post-processing. | REQ-S02 | AC-S02, AC-S04 | TASK-3.1 | - | | |
| TASK-3.3 | **Iterative fix loop** (core weights): *Glyph* gagal *visual review* → harmonisasi ulang (FR-5.3) → interpolasi ulang → validasi ulang. Ulangi hingga <=2% *minor artifact* dan 0 distorsi berat. Ulangan ini melewati *checkpoint* TASK-2.X (≤2% warning); `fail_count = 0` dipertahankan untuk semua core weight. | FR-5.3 | AC-I06 | TASK-3.2 | -     |           |      |
| TASK-3.4 | *Gate Check* cakupan V1: *Stretch weight* (Light, ExtraBold) **TIDAK diproduksi di fase ini** — Custom Build hanya membangun 4 core weight + output existing (Spec §4.6/§8.5, E10). Keputusan kelolosan *stretch weight* (*partial success*, GUD-004) dialihkan ke *release upstream pipeline*. Proyek **tidak dianggap gagal** jika stretch weight tidak rilis di V1. | REQ-I02 | - | TASK-3.3 | -     |           |      |
| TASK-3.X | **VERIFY**: `validate_interpolation.py` melaporkan <= 2% *minor artifact* (`warning`) dan 0 distorsi berat (`fail`) pada **core weights**. *(Catatan: Ambang Phase 3 ini lebih ketat dari PoC yang ≤10%)*. *Advance width* seluruh core weight identik (REQ-I03/I05). *Counter* tidak tertutup, *stem width* proporsional (REQ-I07/AC-I07). *Ligature* berfungsi benar (AC-I05). **Verifikasi Metadata Lapis 1**: one-liner FontForge memeriksa `.sfdir` (os2_weight, familyname, fullname unik per weight dan tidak identik master). | - | AC-I05, AC-I06, AC-I07 | - | - | | |
| NOTE-3.X | **ADD (r5 H1)**: Generalisasi `T_final` — nilai threshold interpolasi di-`set` dari (a) `docs/audit/phase0-experiments-{date}.md` (R-full archive, nilai `T_final` akhir yang divalidasi di PoC) dan (b) `visual-quality-rubric.md` (mapping rubric score → threshold numerik). PoC `configure.py` menggunakan fallback `15.0` **hanya jika** env var `T_FINAL` tidak diset (di-container Stage 1). Runbook mencatat `T_final_source` (file path) di setiap eksekusi untuk reproducibility. | FR-5.3 | AC-I06 | TASK-0.3 | - | | |
| TASK-3.Y | **APPROVAL**: Wait for explicit user confirmation (Final QA) to proceed to Phase 4 — ringkasan keputusan gate visual review dilampirkan di `docs/audit/phase3-visual-review-{date}.md` (keputusan r2; reviewer: Designer A = otoritas final, Designer B untuk shared pool) | -       | -      | -        | -     |           |      |

### Implementation Phase 4: Pipeline Integration & Release (Polish)

- GOAL-005: Integrasi pipeline interpolasi ke Custom Build Workflow di GitHub Actions dan perilisan resmi.

| Task     | Description                                                                                          | Ref ID  | AC Ref | Dep      | Files | Completed | Date |
| -------- | ---------------------------------------------------------------------------------------------------- | ------- | ------ | -------- | ----- | --------- | ---- |
| TASK-4.1 | Modifikasi `Scripts/configure.py` tambah `--form-enable-multi-weight` (boolean, default `false`) — masuk ke `DEFAULTS` dan mapping ke flag driver `--multi-weight` via pola `FORM_KEY_TO_OPTION`/`OPTION_TO_DRIVER_FLAG` (Spec §4.9); tambah properti `EnableMultiWeight` (boolean, default `false`) ke `config.schema.json` (konsistensi, r4 R1). **Catatan r4 R1: manifest mencatat `resolved_options.EnableMultiWeight` sebagai informasi audit — TIDAK dikonsumsi oleh `packaging.sh` (packaging tidak memiliki cabang `ENABLE_MULTI_WEIGHT`; lihat TASK-4.4).** Catatan: `configure.py` TIDAK dilindungi CON-001 (dikelola Custom Build Spec; minor update cross-spec — PRD §8.1, audit C5) | REQ-B02 | AC-B01 | TASK-3.Y | 2     |           |      |
| TASK-4.2 | Modifikasi `Dockerfile` (Stage 1 RUN conditional block) — kontrak RUN chain Spec §4.9 (r3 K1/K2/K3/K4/K8): (i) **strip `--multi-weight`** dari `BUILD_ARGS` sebelum pemanggilan driver agar tidak ditolak `parse_args()` (`_die("unknown flag(s): ...")`): `DRIVER_ARGS=$(printf '%s' "$BUILD_ARGS" \| sed 's/--multi-weight//g')` → `custom_build_driver.py "$FONTS" /build $DRIVER_ARGS`; guard kondisional pakai bentuk portabel `echo "$BUILD_ARGS" \| grep -q -- "--multi-weight"` (TANPA here-string `<<<` — Stage 1 pakai `/bin/sh` (dash) tanpa `SHELL` directive); opsi alternatif: tambah `SHELL ["/bin/bash", "-c"]`; CON-001 tidak tersentuh; (ii) **instalasi pytest + jsonschema tanpa kondisi** di Stage 1: perpanjang `pip3 install --break-system-packages --no-cache-dir future` menjadi `... future pytest jsonschema` (pola existing, satu layer; image single-weight tidak terpengaruh — AC-B03 tetap berlaku); (iii) urutan RUN chain: `detect_incompatibility.py` → `validate_harmonization.py --strict` **untuk KEDUA pasangan master** (Regular↔Bold DAN Italic↔BoldItalic — hasil harmonisasi keduanya ikut di-assembly ke `build/sources/`, r3 K4) → `fontforge ... multi_weight_driver.py` (Medium+SemiBold) → **loop fail-fast `validate_interpolation.py --threshold T_final --fail-fast` per core weight interpolated** (fail = self-intersection / counter tertutup / kontur rusak → exit non-zero, GUD-002 ter-enforce di CI; `warning` ≤2% tidak memblokir build — metrik QA Phase 3) → `RUN pytest tests/` (fail-fast test driver) → `custom_build_driver.py` dengan `FONTS=build/sources`; mode `false` → `FONTS=Sources` → output byte-identical (AC-B03); (iv) **guard eksistensi harmonized sources (r4 R5) — dieksekusi PALING AWAL di RUN chain (sebelum `detect_incompatibility.py`)**: periksa keempat direktori `Sources/Harmonized/{Regular,Bold,Italic,BoldItalic}`; jika salah satu hilang → `echo "::error::multi-weight build requires harmonized sources (Sources/Harmonized/{Regular,Bold,Italic,BoldItalic}); sync upstream or run harmonization first" >&2; exit 1` — error mentah FontForge ("unable to open") tidak informatif bagi fork owner yang belum sync hasil harmonisasi upstream; fail-fast tetap terjaga (GUD-002); prasyarat ini dicatat di README section multi-weight (TASK-4.5). | REQ-B02 | AC-B02 | TASK-4.1 | 1     |           |      |
| NOTE-4.2 | **ADD (r5 B1 critical, B2, H3)**: (i) **Instal `python3-fontforge` via `apt` di Stage 1** (bukan pip): tambah `RUN apt-get update && apt-get install -y --no-install-recommends python3-fontforge && rm -rf /var/lib/apt/lists/*` — agar keempat file test FontForge-dependent (`test_detect_incompatibility.py`, `test_validate_harmonization.py`, `test_validate_interpolation.py`, `test_multi_weight_driver.py`) benar-benar dieksekusi di container (bukan hanya `importorskip`-skip). Koreksi komentar Dockerfile "embedded bindings" → "system FontForge module". Pre-check: `python3 -c "import fontforge"` di awal RUN chain (TASK-0.X). (ii) **`mkdir -p build`** di base Stage 1 (sebelum RUN chain) + **Stage 2 `COPY --from=builder-fontforge /build/build /app/build-reports`** — semua laporan JSON + coverage XML tersedia di Stage 2 untuk di-surfacing via `packaging.sh`. (iii) **Variabel shell `T_FINAL`** — loop fail-fast `validate_interpolation.py --threshold ${T_FINAL} --fail-fast` di mana `T_FINAL` diset di awal RUN chain (default dari `phase0-experiments-{date}.md` + `visual-quality-rubric.md`, bukan hardcode `15.0`). | REQ-B02 | AC-B02 | TASK-4.1 | 1     |           |      |
| TASK-4.3 | Update `.github/workflows/custom-build.yml` — parameter boolean `enable_multi_weight` (default `false`); stretch weight TIDAK dibangun di Custom Build (E10); `timeout-minutes: 360` (batas maksimum platform, CON-007). *Catatan: Tanpa step workflow baru, murni perluasan RUN chain Dockerfile & flag forwarding ke `configure.py`. Step Create GitHub Release existing tidak diubah.* | REQ-B02 | AC-B01 | TASK-4.1 | 1     |           |      |
| NOTE-4.3 | **ADD (r5 H3)**: `.github/workflows/custom-build.yml` — `actions/upload-artifact` step path ditambah `output/reports/**` untuk meng-artifact-lkan laporan validasi JSON (`--threshold` report, `validation-report.json`, `manifest.json`, `coverage.xml`). Path glob: `output/archives/**` dan `output/reports/**`. | REQ-B02, REQ-B05 | AC-B06 | TASK-4.2 | 1     |           |      |
| TASK-4.4 | Modifikasi `Scripts/packaging.sh` — **satu-satunya mode eksplisit adalah `RELEASE_MODE` (definisi r3 K14: env var eksplisit `RELEASE_MODE=1` yang di-invoke maintainer; **CI Custom Build TIDAK PERNAH menyetelnya** — E10). Cabang `ENABLE_MULTI_WEIGHT` DIHAPUS TOTAL (r4 R1)**: verifikasi codebase membuktikan workflow `custom-build.yml` memanggil packaging via `docker run` TANPA `-e`, sehingga cabang env var tidak akan pernah aktif (silent fallback) — perilaku Custom Build kembali ke **zip-all existing** (zip seluruh file di `TTF/`, `OTF/`, `Webfonts/` — otomatis menyertakan Medium/SemiBold/FantasqueSans; AC-B02 terpenuhi tanpa daftar weight eksplisit; archive tetap `fantasque-sans-custom-build.zip`/`.tar.gz`). **Override hinting REQ-I04 (r3 K9 + r4 R1) diputuskan MURNI dari pola nama file**: `NEW_WEIGHTS` = file yang basename-nya match `Medium\|SemiBold\|Light\|ExtraBold` → SELALU di-hint via `ttfautohint`, terlepas `UseHinted`; weight existing (Regular, Bold, Italic, BoldItalic, FantasqueSans) tetap mengikuti `UseHinted` (dibaca dari manifest via jq — pola existing di packaging.sh (`USE_HINTED=$(jq -r '.resolved_options.UseHinted' ...)`). Perilaku rilis upstream (`RELEASE_MODE=1`): archive dipecah ke 3 format terpisah (TTF .zip, OTF .zip, WOFF2 .zip) dengan penamaan `FantasqueSansMono-{VERSION}-{Format}.zip` — **sumber nilai `{VERSION}` = env var eksplisit `VERSION` yang di-invoke maintainer bersama `RELEASE_MODE=1` (`docker run -e RELEASE_MODE=1 -e VERSION=...`); jika `VERSION` kosong → `_die` dengan pesan instruktif (r4 R2 — version adalah keputusan rilis, bukan hasil parsing dokumen/tabel font; manifest `configure.py` hanya memuat `manifest_version`/`workflow_version`/`source_commit`)** — dan menyertakan weight + stretch (jika lolos, FR-6.3). Manifest `resolved_options.EnableMultiWeight` TIDAK dikonsumsi packaging (informasi audit saja, r4 R1). | REQ-B05, REQ-D03 | AC-B06, AC-D01 | TASK-4.2 | 1 |           |      |
| NOTE-4.4 | **ADD (r5 H3)**: `packaging.sh` menyalin `/app/build-reports/*.json` ke `output/reports/` (guard — hanya jika file ada, gunakan `cp -n /app/build-reports/*.json "$OUTPUT_DIR/reports/" 2>/dev/null || true`) sebelum archive assembly. Laporan validasi JSON — termasuk `coverage.xml` (TEST-007), `validation-report.json`, `manifest.json` — tersedia sebagai artifact build untuk di-inspeksi post-run. | REQ-B05 | - | TASK-4.2 | 1     |           |      |
| TASK-4.5 | Update dokumentasi (`README.md` — termasuk *section* "Faux Italic Limitations" dengan tabel kompatibilitas platform + contoh CSS `@font-face`, `Specimen/`). **Tambahan r4 R5: prasyarat harmonized sources (`Sources/Harmonized/{Regular,Bold,Italic,BoldItalic}` — hasil harmonisasi upstream, Phase 2) dicatat di section multi-weight README agar fork owner tahu cara sync sebelum mengaktifkan `enable_multi_weight`** (selaras guard TASK-4.2). | REQ-D04 | AC-S03, AC-S04, AC-S05 | TASK-4.4 | 2     |           |      |
| TASK-4.X | **VERIFY**: Eksekusi lokal *Custom Build pipeline* menggunakan mode docker (`enable_multi_weight=true` dan `false`) sukses dan deterministik (AC-B03). **Packaging (r4 R1)**: archive Custom Build dihasilkan via perilaku zip-all — verifikasi archive berisi file Medium/SemiBold/FantasqueSans secara otomatis (AC-B02); mode release (`RELEASE_MODE=1`) tanpa `VERSION` HARUS gagal dengan pesan instruktif `_die` (r4 R2). Build log menampilkan pesan progres aktual: `"Detecting incompatibilities..."`, `"Harmonizing..."`, `"Interpolating Medium (500)..."`, `"Generating {Weight}..."`, `"ttfautohint"`, `"packaging: ..."` (GUD-003, AC-B05). **Verifikasi Metadata Lapis 2**: Eksekusi `ttx -t name -t OS/2` dan `fc-scan` (Linux) pada TTF final membuktikan metadata weight & nama benar. **WOFF2 ≤ 500 KB (r3 K10)**: budget dihitung atas **6 weight baru** pada set release — Regular, Medium, SemiBold, Bold + Light, ExtraBold (jika diproduksi); Italic, BoldItalic, dan FantasqueSans **tidak** dihitung (weight existing yang sudah dirilis); jika stretch gagal review, jumlah file otomatis berkurang (budget melonggar — tetap kap). Kompatibel `@font-face` CSS. **Hinting**: konsisten untuk semua core weight di Stage 2. | - | AC-B03, AC-B05, AC-I02, AC-I03, AC-D02, AC-D03 | - | - | | |
| TASK-4.Y | **APPROVAL**: Wait for explicit user confirmation to finalize implementation                         | -       | -      | -        | -     |           |      |

### Implementation Phase 5: Buffer — Iteration & Stabilization (Optional)

- GOAL-006: Menyediakan *slack time* untuk iterasi harmonisasi ulang, perbaikan *visual review* lanjutan, dan stabilisasi build. Fase ini hanya aktif jika diperlukan berdasarkan hasil Phase 3 atau 4.
- **Durasi**: 4 minggu (Minggu 15–18, sesuai PRD §9.2 "Buffer Phase")
- **Trigger**: Diaktifkan jika Phase 3 menemukan >2% *glyph* dengan *minor artifact*, jika harmonisasi ulang tambahan diperlukan (termasuk persiapan *stretch weight* untuk *release upstream*), atau jika pipeline build memerlukan stabilisasi pasca-integrasi.

| Task     | Description                                                                  | Ref ID | AC Ref | Dep      | Files | Completed | Date |
| -------- | ---------------------------------------------------------------------------- | ------ | ------ | -------- | ----- | --------- | ---- |
| TASK-5.1 | Harmonisasi ulang *glyph* bermasalah (dari backlog TASK-3.3)                 | FR-5.3 | AC-I06 | TASK-3.4 | 1-2   |           |      |
| TASK-5.2 | Interpolasi ulang + validasi ulang untuk *glyph* yang direvisi               | REQ-I01 | AC-I06 | TASK-5.1 | -     |           |      |
| TASK-5.3 | Stabilisasi dan optimasi build pipeline (jika diperlukan)                    | REQ-B04 | AC-B04 | TASK-4.X | 1-2   |           |      |
| TASK-5.4 | Produksi *stretch weights* (Light 300, ExtraBold 800) di **release upstream pipeline** (eksekusi manual/terisolasi oleh maintainer di luar CI). **Runbook checklist 10 langkah (r3 K14 + r4 R2/MO-1):** (1) tetapkan factor stretch (Designer A + maintainer) → hasil dicatat di `docs/audit/stretch-factor-decision-{date}.md`; (2) jalankan `multi_weight_driver.py --sources Sources --output Sources/Harmonized/Interpolated --enable-light --enable-extrabold --light-factor F --extrabold-factor F` (lokal atau `docker run` image Stage 1); (3) `validate_interpolation.py --threshold T_final --fail-fast` untuk Light & ExtraBold; (4) visual review per stretch (rubric + specimen) → verdict di `tracking.json`; (5) **BARU (r4 R2) — generate `manifest.json` via `configure.py` (form args identik dengan build) sebelum packaging**: pre-flight `packaging.sh` mewajibkan manifest (`[ -f "${INPUT_MANIFEST}" ]`); manifest disertakan di dalam archive dan menjadi dasar release notes; (6) `custom_build_driver.py build/sources /build` — **kontrak `SOURCES_DIR OUTPUT_DIR` (r4 MO-1)**; (7) Stage 2: `ttfautohint` (override REQ-I04 — weight baru selalu di-hint) + WOFF/WOFF2; (8) `packaging.sh` dengan `RELEASE_MODE=1 VERSION=...` → 3 archive `FantasqueSansMono-{VERSION}-{TTF\|OTF\|WOFF2}.zip` — **`VERSION` wajib (guard `_die` jika kosong, r4 R2: sumber nilai version = env var eksplisit, bukan parsing dokumen/tabel font)**; (9) `gh release create` + attach artifact; (10) update README/Specimen. **Trigger (r3 K14 + r4 R2): env var eksplisit `RELEASE_MODE=1` (wajib disertai `VERSION=...`) di-invoke maintainer; CI Custom Build tidak pernah menyetelnya (E10).** Sub-checklist *release readiness*: Keputusan visual review per stretch weight (lolos V1 / gagal V2) → dicatat di `tracking.json`. | REQ-I02 | - | TASK-3.Y | - | | |
| NOTE-5.4 | **ADD (r5 E2/H6)**: Anotasi stage per langkah runbook — (2)–(4) Stage 1 image (FontForge); (5) host runner (Python 3.14, `configure.py`); (6) Stage 2 image (`packaging.sh`); (7) Stage 2 container. **VALIDASI STRETCH PER WEIGHT TERPISAH** — `validate_interpolation.py` dijalankan terpisah untuk Light dan ExtraBold. Exit non-zero pada satu stretch → verdict `fail` di `tracking.json` → weight dikeluarkan dari `WEIGHTS` → lanjut ke weight lain (partial success, GUD-004). **Eksplisit jalur eksklusi**: `--exclude WEIGHT` flag pada `custom_build_driver.py` untuk meng-skip weight yang gagal validasi. | REQ-I01 | AC-I06 | TASK-3.Y | - | | |
| TASK-5.X | **VERIFY**: Seluruh *glyph* lolos validasi akhir; pipeline stabil dan ≤ 240 menit; *stretch weight* yang lolos memenuhi *visual rubric* dan yang gagal dikeluarkan dari V1 (GUD-004). | -       | -      | -        | -     |           |      |
| TASK-5.Y | **FINAL APPROVAL**: Konfirmasi eksplisit bahwa implementasi selesai dan siap rilis — ringkasan keputusan gate stretch (lolos V1 / gagal V2) direkap di `docs/audit/phase3-visual-review-{date}.md` (keputusan r2). | -       | -      | -        | -     |           |      |

## 3. Alternatives

- **ALT-001 (Workflow B - UFO/fontmake)**: Menggunakan ekosistem modern UFO v3 dan `fontmake`. Ditolak untuk V1 karena mengubah struktur repository terlalu radikal; akan dieksplorasi di V2.
- **ALT-002 (Single-file Variable Font)**: Membuat font variabel dengan sumbu `wght`. Ditolak untuk V1 karena memerlukan migrasi penuh ke UFO; V1 hanya berfokus pada static weight.
- **ALT-003 (Stroke modification algorithms)**: Menggunakan algoritma tebal/tipis otomatis seperti `font.changeWeight()`. Ditolak karena merusak asimetri "wibbly-wobbly" (counter space dan handwritten detail).

## 4. Dependencies

- **DEP-001**: FontForge Python 3 API (`fontforge` python module) di ubuntu:26.04.
- **DEP-002**: Tim Type Designer (Designer A & Designer B) untuk melakukan harmonisasi manual yang akan memakan waktu substansial (~140-240 jam).

## 5. Files

- **FILE-001**: `Scripts/detect_incompatibility.py` [NEW]
- **FILE-002**: `Scripts/validate_harmonization.py` [NEW]
- **FILE-003**: `Scripts/validate_interpolation.py` [NEW] — parameter `--threshold DEG` (default 15.0) + `--fail-fast` (r3 K7/K8)
- **FILE-004**: `Scripts/generate_specimen.py` [NEW]
- **FILE-005**: `Scripts/poc_interpolation.py` [NEW] — output ganda: `.sfdir` + TTF (tanpa hinting, r3 K11)
- **FILE-006**: `Scripts/multi_weight_driver.py` [NEW] — kontrak driver propagasi error FontForge; factor interpolasi Medium = 0.5, SemiBold = `0.67` eksak (r4 R3); assembly 7/9 `.sfdir` (r3 K8/K16)
- **FILE-007**: `docs/audit/visual-quality-rubric.md` [NEW]
- **FILE-008**: `Sources/Harmonized/tracking.json` [NEW]
- **FILE-009**: `Scripts/configure.py` [MODIFY] + `config.schema.json` [MODIFY] — tambah `--form-enable-multi-weight`/`EnableMultiWeight` (boolean, default `false`); manifest mencatat `resolved_options.EnableMultiWeight` sebagai info audit, tidak dikonsumsi packaging (r4 R1)
- **FILE-010**: `Dockerfile` [MODIFY]
- **FILE-011**: `.github/workflows/custom-build.yml` [MODIFY]
- **FILE-012**: `Scripts/packaging.sh` [MODIFY]
- **FILE-013**: `README.md` [MODIFY]
- **FILE-014**: `tests/test_detect_incompatibility.py` [NEW] — `pytest.importorskip("fontforge")` (r3 K6)
- **FILE-015**: `tests/test_validate_harmonization.py` [NEW] — `pytest.importorskip("fontforge")` (r3 K6)
- **FILE-016**: `tests/test_multi_weight_driver.py` [NEW] — `pytest.importorskip("fontforge")` (r3 K6)
- **FILE-017**: `docs/audit/phase0-experiments-{date}.md` [NEW]
- **FILE-018**: `docs/audit/parallel-harmonization-simulation-{date}.md` [NEW]
- **FILE-019**: `docs/audit/poc-glyph-list-{date}.md` [NEW]
- **FILE-020**: `docs/audit/stretch-factor-decision-{date}.md` [NEW]
- **FILE-021**: `docs/audit/phase3-visual-review-{date}.md` [NEW]
- **FILE-022**: `tests/fixtures/multi_weight/*.sfdir` [NEW] — fixture dummy input sintetis (deliverable TASK-0.10, konten dikunci); pohon assembly lengkap (Italic/BoldItalic/FantasqueSans) dibangun di temp oleh helper test (r3 K12)
- **FILE-023**: `tests/test_validate_interpolation.py` [NEW] — 5 test case Spec §6.3 (pass, warning, fail, overlay PNG, JSON valid) + `pytest.importorskip("fontforge")` (r3 K5/K6)
- **FILE-024**: `.gitignore` [MODIFY] — tambah `build/` & `Sources/Harmonized/Interpolated/` (r3 K15; owned by TASK-0.7, H2: FILES column TASK-0.7 diupdate eksplisit "2 (FILE-006 + FILE-024)")
- **FILE-025**: `.github/workflows/test-multi-weight.yml` [NEW] — push-gate host runner (trigger push ke `feature/multi-weight-*`; `pip install pytest jsonschema` + `pytest tests/ -v`; file FontForge-dependent di-skip via `importorskip`) — memenuhi Spec §6.6 (r4 R4, TASK-0.14)

## 6. Testing

- **TEST-001**: Idempotency & Determinism Test — Memastikan pipeline menghasilkan byte-identical output pada mode non-multi-weight (`enable_multi_weight=false`).
- **TEST-002**: Automated Validation Scripts — `validate_harmonization.py`, `validate_interpolation.py` dengan laporan JSON terstruktur.
- **TEST-003**: Unit Tests (Spec §6.1, §6.3) — `pytest` untuk `test_detect_incompatibility.py`, `test_validate_harmonization.py`, **`test_validate_interpolation.py`** (5 test case Spec §6.3: pass, warning, fail, overlay PNG, JSON valid — TASK-0.13; memverifikasi `--threshold` & `--fail-fast` exit code), dan `test_multi_weight_driver.py` (11 test case, termasuk `test_semibold_interpolation_factor` — verifikasi `0.67` eksak, toleransi ±0.005 = kelonggaran presisi float, r4 R3). **Keempat file memakai `pytest.importorskip("fontforge")` di level modul** agar gate host runner tidak crash — eksekusi nyata di container Stage 1 (TASK-0.X / TASK-4.2); fixture `tests/fixtures/multi_weight/`, TASK-0.10. **Eksekusi push-gate (r4 R4)**: workflow `test-multi-weight.yml` (TASK-0.14, FILE-025) menjalankan suite ini di host runner pada SETIAP push ke branch `feature/multi-weight-*` (Spec §6.6) — file FontForge-dependent di-skip otomatis; eksekusi nyata dengan FontForge tetap di container Stage 1.
- **TEST-004**: Visual Quality Review — Menggunakan Specimen Sheet HTML dan Visual Quality Rubric (E0.4).
- **TEST-005**: Integration Test — End-to-End Custom Build workflow (`enable_multi_weight=true` dan `false`) via Docker lokal.
- **TEST-006**: Hinting Verification — **Seluruh weight baru (Medium, SemiBold; + Light/ExtraBold di mode `RELEASE_MODE=1`) di-hint di Stage 2 (`ttfautohint`) SELALU, terlepas opsi `UseHinted` (REQ-I04 override, r3 K9)**; weight existing (Regular, Bold, Italic, BoldItalic, FantasqueSans) tetap mengikuti `UseHinted`. Hasil hinting konsisten antar weight (AC-I02).
- **TEST-007**: Unit Test Coverage — Container Stage 1 (TASK-0.X, TASK-4.2) menjalankan `pytest --cov=Scripts --cov-report=term-missing --cov-report=xml:output/reports/coverage.xml` dan menghitung coverage. **Host runner push-gate (FILE-025) TIDAK mengukur coverage** (MO-1: coverage gate = container-only); push-gate hanya smoke gate (H4). Coverage ≥ 90% wajib di-container untuk kelolosan TASK-4.2 RUN chain (Spec §6.7). Laporan XML (`coverage.xml`) di-surfacing ke artifact via `output/reports/` (H3).

## 7. Risks & Assumptions

- **RISK-001**: Harmonization memakan waktu sangat lama (bottle-neck) akibat proses manual yang masif.
- **RISK-002**: Stretch weights (Light, ExtraBold) mungkin tidak memenuhi standar *Visual Quality* karena ekstrapolasi FontForge. Risiko hanya relevan untuk *release upstream pipeline* (definisi: eksekusi manual/terisolasi oleh upstream maintainer di luar CI — lihat TASK-5.4; Custom Build tidak memproduksi stretch weight — E10); dimitigasi via *gate check* kelolosan: gagal → keluar dari V1 → V2 (*partial success*, GUD-004).
- **ASSUMPTION-001**: `Scripts/features.py` dipastikan deterministik dan idempotent, sehingga dapat dipanggil multiple times tanpa mengubah hasil (divalidasi oleh E0.1).

## 8. Related Specifications / Further Reading

- [PRD - Multi-Weight Variants](../docs/prd-20260731-1000-multi-weight-variants.md)
- [Technical Specification - Multi-Weight Variants](../spec/spec-multi-weight-variants.md)
- [ADR-0003: Workflow A - FontForge v1 Interpolation](../docs/adr/0003-workflow-a-fontforge-v1-interpolation.md)
- [Clarification Report: Implementation Plan - Multi-Weight Variants](../docs/audit/clarification-report-implementation-plan-multi-weight-variants-2026-07-31.md)
- [Clarification Report r2: Implementation Plan - Multi-Weight Variants](../docs/audit/clarification-report-implementation-plan-multi-weight-variants-2026-07-31-r2.md)
- [Clarification Report r3: Implementation Plan - Multi-Weight Variants](../docs/audit/clarification-report-implementation-plan-multi-weight-variants-2026-08-01-r3.md)
- [Clarification Report r4: Implementation Plan - Multi-Weight Variants](../docs/audit/clarification-report-implementation-plan-multi-weight-variants-2026-08-01-r4.md)
- [Clarification Report r5: Implementation Plan - Multi-Weight Variants](../docs/audit/clarification-report-implementation-plan-multi-weight-variants-2026-08-01-r5.md)
- [Consistency Audit Report: Plan vs PRD vs Spec (2026-08-01)](../docs/audit/consistency-audit-plan-vs-prd-spec-2026-08-01.md)

## 9. Rollback / Recovery Plan

- Jika fase PoC gagal total, seluruh file baru (`Scripts/detect*.py`, dsb.) akan di-*revert* dengan `git revert`, dan parameter build tidak akan di-*merge* ke `master`.
- Jika Custom Build Workflow memunculkan isu regresi pada environment produksi, parameter `enable_multi_weight` pada `.github/workflows/custom-build.yml` akan dinonaktifkan (`hidden`/`false`) sementara *fix* diimplementasikan, karena fallback `false` akan selalu menjalankan *pipeline* legacy yang byte-identical.

## 10. Changelog

| Version | Date       | Changes                                                                                          |
| ------- | ---------- | ------------------------------------------------------------------------------------------------ |
| 1.11    | 2026-08-01 | Sync r5 (B1–B3, E1–E8, H1–H6, MO-1–MO-3): **B1 (kritis)** — instal `python3-fontforge` di Stage 1 container (apt, bukan pip) agar 4 file test FontForge-dependent dieksekusi penuh, bukan `importorskip`-skip; pre-check `python3 -c "import fontforge"` di TASK-0.X; **MO-1** — `pytest-cov` coverage ≥ 90% di host runner tidak terukur, hanya di container. **B2** — variabel shell `T_FINAL` (dari `phase0-experiments` + `visual-quality-rubric`) di RUN chain validasi, bukan `15.0` default. **B3** — TASK-3.2 tambah langkah lokal `custom_build_driver.py build/sources <tmp>` untuk TTF pra-hinting sebelum FontForge post-processing. **H1** — generalisasi `T_final` (R-full `phase0-experiments` vs PoC `configure.py`). **H2** — FILE-024 owned-by TASK-0.7 (Files 1→2: FILE-006 + FILE-024). **H3** — surfacing laporan JSON ke artifact: `mkdir -p build` Stage 1 → `COPY --from=builder-fontforge /build/build /app/build-reports` Stage 2 → `packaging.sh` copy ke `output/reports/` → workflow upload `output/reports/**`. **H4** — push-gate = smoke gate (host), bukan coverage gate. **H6** — runbook task dianotasi per stage. **E1** — aturan kolaborasi `tracking.json` (no-reformat, union+sort). **E2** — validasi stretch per weight terpisah + jalur eksklusi (GUD-004). **E4 dilewati** — spec amendment (§4.4). **E5** — hmtx copy → unconditional. **E7** — REQ-I06 "dikeluarkan SEMENTARA". **E8** — AC-B02 parenthetical. Amendemen Spec (§4.4/§4.6/§4.8/§4.9/§4.12, REQ-I03/REQ-I06, AC-B02, §6.7, §7, §9.2, §11.2) DIJADWALKAN via `/sdlc-define-specs`. `CONTEXT.md`/ADR: tidak ada. |
| 1.10    | 2026-08-01 | Sinkronisasi laporan klarifikasi r4 (5 resolusi R1–R5 + MO-1): TASK-4.4 ditulis ulang (hapus cabang `ENABLE_MULTI_WEIGHT` — perilaku Custom Build kembali ke zip-all existing; override hinting REQ-I04 via pola nama file `NEW_WEIGHTS`; satu-satunya mode eksplisit `RELEASE_MODE=1`; env var `VERSION` wajib dengan guard `_die` untuk penamaan `FantasqueSansMono-{VERSION}-{Format}.zip`); TASK-4.1 diperjelas (`DEFAULTS` + properti `config.schema.json`; manifest mencatat `resolved_options.EnableMultiWeight` sebagai info audit, tidak dikonsumsi packaging); TASK-4.2 tambah guard eksistensi `Sources/Harmonized/{Regular,Bold,Italic,BoldItalic}` di awal RUN chain (`::error::` + exit 1); TASK-4.5 catat prasyarat harmonized sources di README; TASK-5.4 runbook 9 → 10 langkah (generate `manifest.json` via `configure.py` sebelum packaging + `custom_build_driver.py build/sources /build` + `VERSION` wajib); factor SemiBold dikunci `0.67` eksak (TASK-0.7/0.10/3.1, toleransi ±0.005 = kelonggaran presisi float); TASK-0.14 baru + FILE-025 (workflow push-gate `test-multi-weight.yml` — Spec §6.6); TEST-003 catatan push-gate host vs eksekusi nyata Stage 1; FILE-006 catatan factor 0.67; FILE-009 diperluas (`config.schema.json`); `clarification_reference` rujuk laporan r4; perluasan §8 (laporan r4); rename file ke `-v1.10.md`. |
| 1.9     | 2026-08-01 | Sinkronisasi laporan klarifikasi r3 (16 resolusi K1–K16): RUN chain final Dockerfile (grep portabel tanpa `<<<`, strip `--multi-weight` via `DRIVER_ARGS`, instalasi pytest/jsonschema tanpa kondisi, validasi `--strict` kedua pasangan, loop `validate_interpolation.py --fail-fast` per core weight) TASK-4.2; gate dua-tingkat harmonisasi TASK-2.X (≥98% checkpoint / `fail_count = 0` syarat Phase 3); label 70/25/5 TASK-2.1/2.2/2.3 (bukan batas cakupan); `--threshold`/`--fail-fast` TASK-0.3; output ganda TTF TASK-0.6; `.gitignore` + kontrak driver + jumlah `.sfdir` TASK-0.7; pohon sintetis TASK-0.10; TASK-0.13 baru + FILE-023 (`test_validate_interpolation.py`); eksekasi container Stage 1 + `importorskip` TASK-0.X; protokol dua-pass + review 100% glyph TASK-1.3/1.X; override REQ-I04 + definisi `RELEASE_MODE` TASK-4.4; komposisi budget WOFF2 TASK-4.X; runbook 9 langkah TASK-5.4; TEST-003 diperluas (4 file) + TEST-006 (override hinting); `clarification_reference` rujuk laporan r3; FILE-023/FILE-024; perluasan §1 (GUD-002 enforcement) & §8 (laporan r3); rename file ke `-v1.9.md`. |
| 1.8     | 2026-08-01 | Perbaikan hasil audit konsistensi 2026-08-01: rujuk `audit_reference` ke laporan 2026-08-01 (B1), perbaiki domain language D1 ("release upstream pipeline") & D2 (ALT-002 → "static weight"), perluas §1 dengan GUD-001/GUD-002/CON-003/CON-004/CON-007/CON-008/SEC-001/SEC-002, perluas daftar amendemen upstream di Introduction (B7: Spec §4.7/§4.9; PRD FR-7.2/GH-004 AC#3/GH-005 AC#2/FR-7.1/§9.2 Phase 3/`scripts/`→`Scripts/`) + status aktual Spec v1.4 & PRD v1.4, perluas §8 dengan laporan klarifikasi r2 & audit 2026-08-01, rename file ke `-v1.8.md`. |
| 1.7     | 2026-08-01 | Sinkronisasi laporan klarifikasi r2: catatan `tangent-angle` (TASK-0.2/0.3), definisi operasional warning/fail, perluasan `tracking.json`, `pytest.importorskip` & konten fixture (TASK-0.10), relasi eksperimen E0.2, kriteria worst-offender PoC (TASK-1.1), gate ganda PoC (TASK-1.X), verifikasi metadata dua lapis (TASK-3.X/4.X), RUN pytest di Stage 1 (TASK-4.2), `timeout-minutes: 360` (TASK-4.3), daftar pesan log (TASK-4.X), penetapan factor & definisi release upstream (TASK-5.4), 3 file artefak audit baru; finalisasi gap-fill: keputusan path rubric kanonik `docs/audit/` (TASK-0.5), lampiran ringkasan gate review di TASK-3.Y/5.Y, FILE-022 fixture, koordinasi amendemen Spec/PRD (Introduction), TEST-003 disinkronkan. |
| 1.6     | 2026-07-31 | Sinkronisasi laporan klarifikasi: Phase 3 hanya core weights (stretch → release upstream, E10), hapus TASK-3.2 `ttfautohint` manual (hinting hanya Stage 2, Spec §8.5), renumber TASK-3.x, gate check TASK-3.4 disesuaikan, flag `--enable-light`/`--enable-extrabold`, injeksi metadata driver (TASK-0.7/3.1), kontrak RUN chain Dockerfile & daftar weight packaging per mode (TASK-4.2/4.4), verifikasi hinting Stage 2 di TASK-4.X, tambah TASK-5.4 produksi stretch release upstream, RISK-002 & TEST-006 diperbarui. |
| 1.5     | 2026-07-31 | Observasi minor: tambah verifikasi ligature (AC-I05) ke TASK-3.X, metrik specimen (AC-S04) ke TASK-3.3, archive 3 format (AC-D01) ke TASK-4.4, instalasi font + nama (AC-I02/I03) ke TASK-4.X, perbaiki dependency TASK-2.3 & 2.4 agar konkuren dengan harmonisasi. |
| 1.4     | 2026-07-31 | Konsistensi audit: tambah Buffer Phase 5, unit test tasks, shared pool strategy, visual spot-check, iterative fix loop, perbaiki traceability REQ-ID, pindahkan multi_weight_driver.py ke Phase 0, perkaya TASK-4.X verification, tambah git branch TASK-0.0, perbarui TASK-1.Y dengan 4 jalur keputusan FR-2.5, tambah file unit test & experiment docs, perbarui Testing section. |
| 1.3     | 2026-07-31 | Initial plan draft berdasarkan PRD v1.3 dan Spec v1.3.                                           |