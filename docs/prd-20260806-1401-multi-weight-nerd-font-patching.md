---
title: "PRD — Multi-Weight Nerd Font Patching for Fantasque Sans Mono"
status: DRAFT (Phase 1 — revised v1.2 per Clarification Report Review Iteration 2)
date: 2026-08-06
version: 1.2
phase: SDLC Phase 1 (PRD)
project: Fantasque Sans Mono
author: Senior Product Manager
upstream_discovery: docs/discovery-draft-20260806-0428-multi-weight-nerd-font-patching.md
downstream_phase: "@SpecificationArchitect → /sdlc-define-specs"
related_docs:
  - docs/discovery-draft-20260806-0428-multi-weight-nerd-font-patching.md
  - docs/audit/clarification-report-prd-multi-weight-nerd-font-patching-2026-08-06.md
  - docs/audit/clarification-report-prd-multi-weight-nerd-font-patching-2026-08-06-r2.md
  - docs/prd-20260731-1000-multi-weight-variants.md
  - docs/adr/0002-multi-stage-docker-deferred-engine-port.md
  - CONTEXT.md
target_release_window: After Multi-Weight Variants V1 implementation completes, no fixed calendar date
license_impact: Base Flavor remains SIL Open Font License (OFL-1.1) unchanged; the Nerd Font Flavor additionally bundles third-party icon-set licenses with an attribution manifest
---
<!-- markdownlint-disable -->

## PRD: Multi-Weight Nerd Font Patching for Fantasque Sans Mono

## 1. Product overview

### 1.1 Document title and version

- **PRD**: Multi-Weight Nerd Font Patching for Fantasque Sans Mono
- **Version**: 1.2 (DRAFT — remediated per Clarification Report, Review Iteration 2; Edits 9–13 applied)
- **SDLC Phase**: Phase 1 (PRD) — upstream: Phase 0 Discovery (approved 2026-08-06); downstream: Technical Specification (`/sdlc-define-specs`) — Clarification Checkpoint passed (Review Iteration 2, Readiness Score 90/100, 2026-08-06)
- **Date**: 2026-08-06
- **Author**: Product Manager PRD persona
- **Upstream artifacts**:
  - Discovery Draft: [`docs/discovery-draft-20260806-0428-multi-weight-nerd-font-patching.md`](/docs/discovery-draft-20260806-0428-multi-weight-nerd-font-patching.md) (Rev 4, revised per Clarification Report Edit 8; supersedes Rev 3)
  - Parent PRD: [`docs/prd-20260731-1000-multi-weight-variants.md`](/docs/prd-20260731-1000-multi-weight-variants.md) (v1.4)
- **Recorded product decisions (2026-08-06, session clarification)**:
  - Discovery draft Rev 3 approved in full as the upstream artifact.
  - Target audience: end-user developers and terminal users who consume release artifacts.
  - Primary business goal: parity with the Nerd Fonts ecosystem.
  - Flavor activation: **automatic within the multi-weight pipeline** (conditional on `enable_multi_weight = true`; default `false` per `custom-build.yml` and `config.schema.json`) — the Nerd Font Flavor is produced by every multi-weight-enabled build; no new `workflow_dispatch` input is added. Builds with multi-weight disabled keep today's exact behavior and produce no Nerd Font artifacts (see FR-7.1).
  - Fallback policy: the 427-glyph native fallback is **accepted** for release and MUST be disclosed in release notes and the README (see FR-2.4).
  - Stretch weight fallback source-master policy (T-1, Clarification Review Iteration 2): Light 300 uses the Regular outline directly (extrapolation from Regular); ExtraBold 800 uses the Bold outline directly (extrapolation from Bold) — see FR-2.2, FR-2.4, FR-4 intro.
  - 427 count scope (T-2, Clarification Review Iteration 2): the 427 incompatible glyphs refer to the **Regular↔Bold master pair** only; Italic↔BoldItalic harmonization remains a V2 target and does not affect V1 disclosure — see FR-2.1.
- **Target release window**: After Multi-Weight Variants V1 implementation completes, no fixed calendar date
- **License impact**: Base Flavor stays under SIL Open Font License (OFL-1.1) with no change. The Nerd Font Flavor additionally bundles icon glyph data from upstream Nerd Fonts v3.5.0, which aggregates multiple icon sets; the flavor MUST carry the required upstream licenses and attribution (FR-6).

### 1.2 Product summary

The Multi-Weight Variants pipeline has reached a shape-preserving ceiling: 427 native glyphs (374 contour/topology mismatches + 53 not equalizable by the available exact operations) remain incompatible between masters and are slated for type-designer review. This PRD is a deliberate scope change to the approved Multi-Weight Variants initiative. It introduces a **Native Glyph Fallback** policy so the interpolation pipeline can produce the new weights deterministically today, and it adds a second product flavor — the **Nerd Font Flavor** — generated by running the pinned Nerd Fonts Patcher v3.5.0 after the base fonts are built.

The fallback copies the **source-master outline** into the affected derived weights: **Regular outline for Medium/SemiBold/Light; Bold outline for ExtraBold** — so the output remains renderable and deterministic; it is a known visual limitation that must be disclosed, not a repair of the 427 glyphs. The Nerd Fonts Patcher is **additive on native glyphs**: it inserts icon glyphs at Private Use Area codepoints and never replaces native Latin, Greek, Cyrillic, digit, or punctuation glyphs. Overwrites are permitted only at the per-weight AuthorizedOverwriteAllowlist (default = SourcePopulated: Regular 69, Bold 69, Italic 75, BoldItalic 72, union 75) — and only in the Nerd Font Flavor; the actual overwrite set is verified against this allowlist by the collision report (FR-4). The Base Flavor is never touched.

The Nerd Font Flavor is produced **automatically inside the existing build pipeline whenever multi-weight is enabled** (`enable_multi_weight = true`; a dedicated Docker stage, no new workflow inputs) for every released monospace weight, with full artifact and format parity (TTF, OTF, WOFF, WOFF2, independent manifests, checksums, archives, family names, and release notes). The product goal is parity with the Nerd Fonts ecosystem: developers who standardize on Nerd Fonts icon workflows can adopt Fantasque Sans Mono — in every weight the multi-weight initiative ships — without patching fonts manually.

**Activation scope:** The Nerd Font Flavor is produced automatically **only when `enable_multi_weight = true`** (default `false` per `custom-build.yml` lines 45–46 and `config.schema.json` lines 28–30). Builds with multi-weight disabled produce only the Base Flavor — no Nerd Font artifacts, no patcher execution (FR-7.1).

## 2. Goals

### 2.1 Business goals

- **Parity with the Nerd Fonts ecosystem**: Make Fantasque Sans Mono available as an official Nerd Font flavor with the complete v3.5.0 icon set, so developers who standardize on Nerd Fonts icon workflows (Powerline, Devicons, Font Awesome, Material Design Icons, and friends) can adopt the font without a second thought.
- **Remove the community patching burden**: Today users patch Fantasque Sans Mono themselves or rely on third-party patched forks; an official flavor removes fragmentation, support load, and inconsistent quality across unofficial builds.
- **Complete the Multi-Weight Variants offering**: Every weight the multi-weight initiative ships (core weights, and stretch weights that pass their release gates) is also available with icons in one bundle, strengthening the competitive position of the font among programming fonts.

### 2.2 User goals

- **Developer & terminal user**: Install one Nerd Font Flavor that brings the complete icon set across every released weight, so terminal, tmux, and IDE setups render icons correctly without manual patching.
- **Fork Owner**: Receive both the Base Flavor and the Nerd Font Flavor from every Custom Build automatically, without learning a new configuration option.
- **Type Designer**: Keep a verifiable quality gate — collision reports and visual review of the small overwrite surface — so the flavor never damages Fantasque's native glyphs silently.

### 2.3 Non-goals (Out of Scope)

- **Repairing the 427 incompatible native glyphs**: The fallback copies the source-master outline into affected new weights (Regular for Medium/SemiBold/Light, Bold for ExtraBold); the manual harmonization backlog stays open and is handled by type designers separately. The patcher is not a repair mechanism.
- **Modifying the Base Flavor**: The patcher never mutates Base Flavor artifacts, `.sfdir` sources, or existing webfont outputs. In multi-weight builds the fallback (FR-2) intentionally changes the outlines of the 427 affected glyphs in the new weights only — that change is disclosed, not silent; builds with multi-weight disabled keep today's artifact set and behavior unchanged (FR-7.1).
- **New CI surface**: No new workflow, job, or Actions step. Patching runs inside the existing `docker build` step via a dedicated Docker stage; `test-multi-weight.yml` (a pytest-only smoke gate) is unaffected.
- **Proportional (non-monospace) Nerd Font variant**: Only monospace weights are covered by this proposal.
- **Patcher version drift**: Only the pinned, checksum-verified Nerd Fonts Patcher v3.5.0; never the moving `master` branch and no unpinned upgrades.
- **Curating or redesigning bundled icon sets**: Icon glyphs are bundled as-is from the upstream v3.5.0 release with their licenses.
- **Variable Font (VF)**: Remains a V2 target (see parent PRD GH-006).
- **Changing the protected legacy toolchain**: `CON-001` stands — no modification to legacy sources or `Scripts/features.py`; all work goes through the Docker build stages and existing packaging scripts.

## 3. User personas

### 3.1 Key user types

- **Developer & terminal user** — consumes release artifacts; wants icons to "just work".
- **Fork Owner** — triggers Custom Builds and expects feature parity with the upstream release.
- **Type Designer** — owns visual quality of the fallback and the collision surface.
- **Build Engineer / Maintainer** — owns the pipeline, the release, and the licensing manifest.

### 3.2 Basic persona details

- **Developer & terminal user**: Uses Nerd Fonts in terminals, tmux status lines, and IDEs (VS Code, JetBrains, Neovim with Powerline/Devicons setups). Downloads a font once and expects every icon to render. Sensitive to broken glyphs and to fonts that require manual patching.
- **Fork Owner**: Maintains a fork of Fantasque Sans Mono and triggers Custom Builds for personalized variants. Values minimal configuration surface and outputs that mirror the upstream release structure.
- **Type Designer**: Reviews the fallback glyphs against the Visual Quality Rubric and validates that the per-weight overwrite surface — bounded by the AuthorizedOverwriteAllowlist, default = SourcePopulated (union 75; see FR-4) — is an acceptable trade-off in the Nerd Font Flavor only.
- **Build Engineer / Maintainer**: Keeps the deterministic-build contract (pinned toolchain, checksums, reproducible outputs), publishes releases, and maintains the license/attribution manifest for bundled icon sets.

### 3.3 Role-based access

- **Public (end users)**: Download both flavors from GitHub Releases; report issues; no write access.
- **Fork Owner**: Trigger Custom Builds via `workflow_dispatch`; receive both flavors automatically when multi-weight is enabled; no new inputs introduced by this feature.
- **Type Designer**: Read access to validation reports, collision reports, and specimen outputs; authority over visual acceptance of fallback glyphs and the collision surface.
- **Maintainer**: Publish releases; approve the license/attribution manifest; gate stretch weights through the Visual Quality Rubric.

## 4. Functional requirements

### FR-1: Two Product Flavors (Priority: CRITICAL)

The product ships two independent flavors. The **Base Flavor** is the existing Fantasque Sans Mono output, and the **Nerd Font Flavor** is the same fonts augmented with the complete Nerd Fonts icon set.

- **FR-1.1**: In single-weight mode (`enable_multi_weight = false`), the Base Flavor MUST remain exactly as it is today — same artifacts, naming, manifest structure, and rollback path (regression gate FR-7.1, SM-T4). In multi-weight mode, the Nerd Font patcher MUST NOT mutate, replace, or rename Base Flavor outputs or their protected `.sfdir` sources (`CON-001`); the only Base Flavor changes in that mode are the disclosed native-glyph fallbacks of FR-2, which belong to the multi-weight pipeline itself and are not patcher effects.
- **FR-1.2**: The Nerd Font Flavor MUST be produced as a separate output namespace: distinct family names, file names, archives, manifests, and checksums (see FR-5). It MUST never write over Base Flavor TTF, OTF, WOFF, WOFF2, or `.sfdir` files.
- **FR-1.3**: Flavor activation is **automatic within the multi-weight pipeline** (conditional on `enable_multi_weight = true`): every multi-weight-enabled build MUST produce both flavors, with no new `workflow_dispatch` input. Builds with `enable_multi_weight = false` MUST NOT produce Nerd Font artifacts and MUST retain today's exact behavior (FR-7.1, SM-T4).

### FR-2: Native Glyph Fallback (Priority: CRITICAL)

The 427 native glyphs that remain incompatible between masters (374 contour/topology mismatches + 53 not equalizable) are not repaired by this feature; they are handled by a documented fallback so interpolation produces deterministic, renderable output.

- **FR-2.1**: The glyph status boundary MUST distinguish three outcomes: `compatible` (normal interpolation), `fallback_regular` (Regular outline substituted), and `needs_harmonization` (still tracked in `tracking.json` for manual type-designer work). The **427 glyphs** currently tracked as incompatible in **`Sources/Harmonized/tracking.json`** refer to the **Regular↔Bold master pair** (the active interpolation pair in V1). Italic↔BoldItalic harmonization remains a V2 target and does not affect V1 disclosure. The 427 glyphs are classified `fallback_regular` by this policy.
- **FR-2.2**: The fallback MUST be applied only to a temporary interpolation input derived from the harmonized masters — protected masters are never modified. For each fallback glyph used by the Regular↔Bold interpolation, the Bold-side outline MUST be made identical to the Regular outline so the output glyph resolves to the Regular outline by construction, deterministically.

  **For stretch weights (extrapolation, not interpolation):**
  - **Light 300** (extrapolation from Regular, factor < 0) — the Regular outline is used directly; no substitution is needed because extrapolation involves only one master.
  - **ExtraBold 800** (extrapolation from Bold, factor > 1.0) — the Bold outline is used directly.

  The 'fallback' terminology in FR-2 applies to interpolation (Medium/SemiBold); for stretch weights, the 427 glyphs are simply the corresponding master outlines (Regular in Light, Bold in ExtraBold), not a 'fallback substitution'.
- **FR-2.3**: Validation output MUST record the fallback count, glyph names, source master, and reason per weight, and MUST distinguish structural interpolation passes from fallback glyphs. This report is part of the release evidence.
- **FR-2.4**: The fallback MUST be disclosed to users as a known visual limitation: release notes and README MUST state that some glyphs in Medium/SemiBold/Light render with the **Regular outline**, and the corresponding glyphs in ExtraBold render with the **Bold outline**. The Visual Quality Rubric MUST classify the weight inconsistency as an accepted limitation for release (decision recorded 2026-08-06) — not as a silent pass.

### FR-3: Nerd Font Icon Augmentation (Priority: CRITICAL)

The Nerd Fonts Patcher adds icon glyphs to the font. The patcher is **additive on all native Latin, Greek, Cyrillic, digit, and punctuation glyphs** (never removes, alters, or replaces) and **overwrites only at the per-weight PUA allowlist** (Regular 69, Bold 69, Italic 75, BoldItalic 72 codepoints, union 75; allowlist default = full SourcePopulated, see FR-4) in the Nerd Font Flavor only. Overwrites outside the allowlist constitute drift and fail the build gate (FR-4.3). Added codepoints (PinnedIconInventory codepoints where SourcePopulated is empty) are informational, not a gate. Icon-set attribution for the collision ranges is pending the pinned v3.5.0 audit (Spec phase, O-3); no icon-set-to-range mapping in this PRD is asserted as verified.

- **FR-3.1**: The patcher MUST be invoked in a mode that merges the complete icon set into the font without removing, altering, or replacing native Latin, Greek, Cyrillic, digit, and punctuation glyphs. Only codepoint collisions (FR-4) may result in overwrites, and only in the Nerd Font Flavor.
- **FR-3.2**: The patcher MUST be pinned to the official Nerd Fonts Patcher release `v3.5.0` with a verified checksum. Tracking the moving `master` branch is forbidden (reproducibility contract).
- **FR-3.3**: The patcher MUST be invoked with the `--complete` policy so the full available icon set of v3.5.0 is included.
- **FR-3.4**: The patcher MUST be applied to every released monospace weight: Regular, Bold, Italic, BoldItalic, Medium, SemiBold, and any stretch weight that passes its release gate. A weight missing from the Nerd Font Flavor while present in the Base Flavor is a release blocker (parity, FR-5).

### FR-4: Collision Policy & Collision Report (Priority: HIGH)

The **per-weight PUA SourcePopulated inventory** (populated codepoints in master source, verified by Encoding inspection of `.glyph` files across the full PUA range U+E000–U+F8FF) is: Regular 69, Bold 69, Italic 75, BoldItalic 72 codepoints; union across all masters = 75 codepoints (composition: U+E000–E00A [11] + U+E035–E039 [5] + U+E03C–E03F [4] + U+E0A0–E0A2 [3] + U+E0B0–E0B3 [4] + U+E0E2–E0E4 [3] + U+E100–E12C [45]). The inventory comprises: (a) U+E000–E00A (stylistic alternates, `k.noloop` family, Serbian alternate ligatures — content varies per master; the icon set believed to collide here is Pomicons — **hypothesis pending the pinned v3.5.0 audit**), (b) U+E035–E039 + U+E03C–E03F programming ligature prefixes/suffixes, (c) U+E0A0–E0A2 + U+E0B0–E0B3 native Powerline symbols, (d) U+E0E2–U+E0E4 per-master extensions (Bold +E0E3,+E0E4; Italic +E0E2,+E0E3,+E0E4; Regular/BoldItalic none), (e) U+E100–E12C programming ligatures (45 codepoints, all masters; Discovery's "no icon set here" is **hypothesis pending the pinned audit**).

The **AuthorizedOverwriteAllowlist(weight)** is the policy set the patcher is authorized to overwrite; **default = SourcePopulated(weight)**, and MAY be narrowed in Spec phase (e.g., excluding U+E100–E12C if the team so decides).

**Stretch weight SourcePopulated inheritance:** Light 300 (extrapolation from Regular) inherits Regular's full-PUA SourcePopulated inventory (**69 codepoints**); ExtraBold 800 (extrapolation from Bold) inherits Bold's (**69 codepoints**). The `AuthorizedOverwriteAllowlist(Light)` defaults to inherited Regular inventory; the `AuthorizedOverwriteAllowlist(ExtraBold)` defaults to inherited Bold inventory. The collision report gate (FR-4.3) and the drift gate (`ObservedOverwrite ⊆ AuthorizedOverwriteAllowlist`) apply to stretch weights identically — any overwrite outside the inherited allowlist fails the gate. The Spec phase MUST verify the actual PUA inventory in the extrapolated font (FontForge extrapolation may introduce minor differences from the source master).

The **drift gate** is `ObservedOverwrite(weight, build) ⊆ AuthorizedOverwriteAllowlist(weight)`. **ExpectedOverwrite(weight) = SourcePopulated(weight) ∩ PinnedIconInventory** is the predicted set, reported for comparison (informational; PinnedIconInventory is the pinned Nerd Fonts v3.5.0 icon codepoint set, audited in Spec phase per O-3). Codepoints outside the allowlist MUST NOT be overwritten; any such overwrite is drift and fails the build gate.

- **FR-4.1**: In the Nerd Font Flavor only, the patcher MAY overwrite existing glyphs at the per-weight AuthorizedOverwriteAllowlist (default = SourcePopulated, see FR-4 intro). Any overwrite outside this list is drift and MUST fail the build gate.
- **FR-4.2**: The Base Flavor MUST never be overwritten by any collision policy, in any build mode.
- **FR-4.3**: A collision report MUST be generated per weight, comparing Patched TTF (output of Nerd Fonts Patcher) against Base TTF (input to patcher). The report MUST record, per weight:
  1. **ObservedOverwrite** — the actual set of codepoints whose glyph changed in this build (derived from the diff between Base TTF and Patched TTF).
  2. **ExpectedOverwrite** — `SourcePopulated(weight) ∩ PinnedIconInventory`, computed at build time from the pinned Nerd Fonts v3.5.0 icon inventory (informational, for comparison).
  3. **Added** — codepoints where PinnedIconInventory has an icon but SourcePopulated is empty (patcher added a new glyph; informational only).
  4. **Drift gate** — `ObservedOverwrite(weight, build) ⊆ AuthorizedOverwriteAllowlist(weight)` MUST hold (equivalently, `ObservedOverwrite \ AuthorizedOverwriteAllowlist = ∅`). Any drift blocks the release.
- **FR-4.4**: The overwritten surface MUST undergo visual review by a Type Designer before release, so valid Fantasque glyphs are not changed unknowingly.

### FR-5: Artifact & Format Parity (Priority: HIGH)

A patched TTF without regenerated webfont outputs is incomplete; both flavors must be fully packaged.

- **FR-5.1**: Both flavors MUST ship TTF, OTF, WOFF, and WOFF2 for every released weight, plus their manifests.
- **FR-5.2**: The Nerd Font Flavor MUST have independent manifests, checksums, archive names, family names, and release notes. No file or checksum from the Base Flavor MAY be reused for the Nerd Font Flavor.
- **FR-5.3**: Naming follows the Nerd Fonts ecosystem convention: family name "Fantasque Sans Mono Nerd Font"; files named `FantasqueSansMonoNerdFont-{Weight}.{ext}` (e.g., `FantasqueSansMonoNerdFont-Medium.ttf`). Base Flavor names remain `FantasqueSansMono-{Weight}.{ext}`. The two naming sets MUST NOT collide.
- **FR-5.4**: All Nerd Font OTF and webfont outputs MUST be generated from the patcher outputs (patched TTF as the source of truth); shipping a webfont derived from an unpatched base font in the Nerd Font Flavor is a defect.

### FR-6: Licensing & Attribution (Priority: HIGH)

Nerd Fonts v3.5.0 aggregates multiple icon sets (Pomicons, Powerline Symbols, Seti-UI, Devicons, Font Awesome, Material Design Icons, and others). The flavor must carry the required upstream licenses.

- **FR-6.1**: The Nerd Font Flavor package MUST include the upstream license texts and an attribution manifest covering 100% of the bundled icon sets enabled by `--complete`. The manifest MUST list each icon set, its license, and its source.
- **FR-6.2**: The Base Flavor package MUST remain unchanged and carry only its existing contents (OFL-1.1 for Fantasque outlines). No license regression in either flavor.

### FR-7: Backward Compatibility (Priority: CRITICAL)

- **FR-7.1**: Builds with `enable_multi_weight = false` MUST retain the current base output behavior — the same artifact set, naming, and pipeline behavior as today, with no Nerd Font Flavor and no patcher execution. This is a hard regression gate (behavioral/non-mutation, SM-T4). Note: byte-identity is NOT a requirement — build manifests embed a dynamic `build_timestamp` by design (`Scripts/configure.py`), and the build driver only mitigates FontForge output non-determinism via `SOURCE_DATE_EPOCH` (`Scripts/custom_build_driver.py`).
- **FR-7.2**: Enabling the Nerd Font Flavor MUST NOT mutate, replace, or invalidate Base Flavor artifacts produced in the same build.
- **FR-7.3**: The `test-multi-weight.yml` workflow (pytest-only smoke gate) MUST remain unaffected. No new workflow, job, or Actions step is introduced by this feature (FR-8).

### FR-8: Pipeline Placement (Priority: HIGH)

The patcher runs as a dedicated Docker build stage inside the existing `docker build` step of `custom-build.yml` — the repository's only artifact-producing workflow.

- **FR-8.1**: A dedicated Docker stage (`builder-nerd-patcher`) MUST run between Stage 1 (`builder-fontforge`) and Stage 2 (`final`), deriving from Stage 1 so it inherits the FontForge runtime without adding FontForge to the final packaging image.
- **FR-8.2**: The patched outputs MUST be copied into Stage 2 via `COPY --from=builder-nerd-patcher` before `Scripts/packaging.sh` runs, and MUST be consumed by the existing hinting, conversion, and packaging steps.
- **FR-8.3**: The workflow file (`custom-build.yml`) MUST keep its current step structure — no new workflow, job, or Actions step. `test-multi-weight.yml` is untouched.
- **FR-8.4**: Total build duration (Base Flavor + Nerd Font Flavor, all weights) MUST complete within **≤ 300 minutes** on a GitHub Actions free-tier runner (the parent PRD caps the multi-weight pipeline at ≤ 240 minutes; patching and regenerated webfonts carry the headroom). Exceeding the cap is a release blocker.

### FR-9: Post-Patch Validation Gates (Priority: MEDIUM)

- **FR-9.1**: Before publication, the Nerd Font Flavor MUST pass post-patch gates: vertical metrics (ascent, descent, lineGap) MUST be documented in the collision report for audit — the gate asserts the actual values are recorded for post-build inspection, not equivalence with the base weight (pinned source citation for the patcher's metric behavior is pending in Spec phase per O-4; until cited, this is an audit gate, not a drift gate); monospace advance widths preserved; family naming correct per FR-5.3; and output completeness (every released weight in every format).
- **FR-9.2**: The patcher MUST write exclusively into its own output namespace inside the build stage; it MUST NOT write to base font directories, `.sfdir` sources, or existing webfont output paths.
- **FR-9.3a (Collision report)**: Validation gate MUST compare Patched TTF (output of Nerd Fonts Patcher) against Base TTF (input to patcher) per weight, and record: (i) ObservedOverwrite (codepoint collisions), (ii) changes to native glyphs **outside the AuthorizedOverwriteAllowlist (must be zero — drift gate)**; changes at the allowlist codepoints (ObservedOverwrite) are recorded as such, not as drift, (iii) family naming (must match FR-5.3), (iv) output completeness (every released weight in every format). The record accompanies the release. Drift gate per FR-4.3.
- **FR-9.3b (Webfont integrity)**: Validation gate MUST compare Hinted TTF (output of ttfautohint) against Patched TTF pre-hinting and against WOFF/WOFF2 outputs, to verify hinting fidelity and compression losslessness. The record accompanies the release.

## 5. User experience

### 5.1 Entry points & first-time user flow

- **Release page**: Each release presents two clearly labelled groups of archives — the Base Flavor (`FantasqueSansMono-*`) and the Nerd Font Flavor (`FantasqueSansMonoNerdFont-*`). Release notes explain the difference in one line and link to the icon-usage section of the README.
- **README**: A "Nerd Font Flavor" section describes installation, icon usage in terminals/IDEs, the collision policy, and the native-glyph fallback limitation (FR-2.4).
- **Custom Build**: No new entry point — the Nerd Font Flavor is included automatically whenever multi-weight is enabled (FR-1.3).

### 5.2 Core experience

- **Install**: The user downloads the Nerd Font Flavor archive for their platform format (TTF or OTF), installs it through the standard OS mechanism (Windows right-click → Install, macOS Font Book, Linux `~/.local/share/fonts/`), and selects **"Fantasque Sans Mono Nerd Font"** in their terminal, tmux, or IDE.
- **Icons render**: Powerline, Devicons, Font Awesome, Material Design Icons, and the rest of the v3.5.0 icon set render at their standard codepoints — no patching, no configuration.
- **Weights work as expected**: Medium, SemiBold, Light, and ExtraBold carry the same icon set as Regular and Bold; the 427 fallback glyphs render with the **source-master outline (Regular for Medium/SemiBold/Light, Bold for ExtraBold)**, which is disclosed and visible in the specimen.

### 5.3 UI/UX highlights & Edge cases

- **Side-by-side install is safe**: Distinct family names ("Fantasque Sans Mono" vs "Fantasque Sans Mono Nerd Font") mean both flavors can be installed at once without name collision or confusion.
- **Apps without PUA support**: Applications that cannot render Private Use Area codepoints show blank/tofu boxes for icons — standard Nerd Fonts behavior, disclosed in the README; it never affects native text.
- **Fallback disclosure**: The weight inconsistency of the 427 fallback glyphs is surfaced in release notes and the README rather than hidden; the Visual Quality Rubric classifies it as an accepted limitation.
- **Stretch weights**: Light/ExtraBold appear in the Nerd Font Flavor only if they pass their release gates; until then the flavor covers the core weights only, and release notes say so explicitly.

## 6. Narrative

Alex, a backend developer, has standardized his entire toolchain on Nerd Fonts — tmux status lines, VS Code file icons, and Neovim bufferline all depend on icon codepoints. He has always loved Fantasque Sans Mono's "wibbly-wobbly" handwriting feel, but every attempt to use it meant downloading a community-patched build of uncertain provenance or running the patcher himself. With the Nerd Font Flavor, he downloads one archive, installs "Fantasque Sans Mono Nerd Font", and his whole icon setup works — in Regular, Bold, and now Medium and SemiBold too. The release notes tell him honestly which glyphs use the Regular outline as a fallback, so nothing surprises him later.

Rina, a designer, maintains a design system where code blocks must match the brand. For her, the two-flavor split is the safety guarantee: the Base Flavor stays exactly as it has always been, while the Nerd Font Flavor gives her team the icons the developer tooling expects — without ever compromising the native glyphs.

Behind the scenes, Bayu, the build engineer, keeps the promise of determinism: the patcher is pinned and checksum-verified, the collision report confirms zero drift from the per-weight allowlist every build, and the base artifacts remain untouched as the rollback and comparison point. The quality gate is visible, not assumed.

## 7. Success metrics

### 7.1 User-centric metrics

- **SM-U1 — Nerd Font Flavor adoption**: ≥ 30% of release asset download events target Nerd Font Flavor archives within 3 months post-release (measured via the GitHub Release API `releases/{id}/assets` download counts).
- **SM-U2 — Positive sentiment**: ≥ 80% positive comments or reactions on social media (Twitter/X, Reddit r/typography, Hacker News) and GitHub issues within the first month post-release, with no confirmed report of native glyph damage.
- **SM-U3 — Self-reported usage**: ≥ 5 GitHub issues or discussions reporting successful production use of the Nerd Font Flavor (installed with icons rendering) within 3 months post-release.

### 7.2 Business metrics

- **SM-B1 — Release download growth**: ≥ 50% increase in total release downloads compared to the 3-month average before the Nerd Font release.
- **SM-B2 — Support burden reduction**: ≥ 25% decrease in issues asking how to patch Fantasque Sans Mono manually or requesting a pre-patched build, within 6 months post-release.
- **SM-B3 — Ecosystem parity**: The Nerd Font Flavor is installable and functional with standard Nerd Fonts ecosystem tooling (upstream patcher validation runs clean against our artifacts), confirming integration rather than a one-off archive.

### 7.3 Technical metrics

- **SM-T1 — Collision drift**: Zero drift across every released weight — every collision report verifies that `ObservedOverwrite(weight, build) ⊆ AuthorizedOverwriteAllowlist(weight)` (equivalently, `ObservedOverwrite \ AuthorizedOverwriteAllowlist = ∅`). Per-weight SourcePopulated inventories (full PUA U+E000–U+F8FF): **Regular 69, Bold 69, Italic 75, BoldItalic 72 (union 75 across all four masters); Light 300 inherits Regular (69); ExtraBold 800 inherits Bold (69)**. AuthorizedOverwriteAllowlist defaults to SourcePopulated (or its inherited value for stretch weights per FR-4 intro) and MAY be narrowed in Spec phase (FR-4). ExpectedOverwrite (= `SourcePopulated ∩ PinnedIconInventory`) is reported for comparison but is NOT the gate (FR-4.3).
- **SM-T2 — Artifact parity**: 100% of released weights in the Nerd Font Flavor ship TTF, OTF, WOFF, and WOFF2 with valid independent manifest checksums (FR-5).
- **SM-T3 — Build duration**: Total build (Base + Nerd Font Flavor) completes in ≤ 300 minutes on a GitHub Actions free-tier runner (FR-8.4).
- **SM-T4 — Backward compatibility regression gate**: A single-weight build (`enable_multi_weight = false`) produces the same artifact set, naming, and structure as the pre-feature baseline — no Nerd Font artifacts, no patcher side effects. Manifests may differ in `build_timestamp` by design (behavioral regression gate, FR-7.1).
- **SM-T5 — License manifest completeness**: 100% of icon sets bundled by the `--complete` policy are covered by the attribution manifest shipped with the Nerd Font Flavor (FR-6.1).

## 8. Technical considerations (Input for Engineering Team)

### 8.1 Integration points

- **Dockerfile**: A new dedicated stage `builder-nerd-patcher` derives from Stage 1 (`FROM builder-fontforge`) so it inherits the verified FontForge runtime; Stage 2 (`final`) gains a `COPY --from=builder-nerd-patcher` for the patcher outputs before `Scripts/packaging.sh` runs. Stage 2 itself stays free of FontForge.
- **`Scripts/packaging.sh`**: Gains a flavor branch — Nerd Font packaging, independent manifest/checksums, archive naming per FR-5. Base packaging behavior is unchanged.
- **`.github/workflows/custom-build.yml`**: Unchanged step structure (FR-8.3). Activation flows through the existing multi-weight pipeline inputs; no new `workflow_dispatch` input (FR-1.3).
- **Validation scripts**: `validate_harmonization.py` / `validate_interpolation.py` gain the `fallback_regular` outcome and report fields (FR-2.1, FR-2.3); the interpolation driver gains the fallback preparation path (FR-2.2).
- **Post-patch gates**: Collision report (FR-4.3, FR-9.3a) and webfont-integrity validation (FR-9.3b) comparing patched vs base inputs (metrics, widths, naming, completeness).
- **Specimen & docs**: Specimen generator and README updated for the Nerd Font Flavor, fallback disclosure, and icon usage (FR-2.4, §5).
- **Architecture docs**: `docs/ARCHITECTURE.md` is known to drift from the current harmonization/multi-weight state; it MUST be refreshed (via `sdlc-map-architecture`) before the Specification phase. The patcher placement decision requires a new ADR (ADR-0004, Triple-Gate validated) during the Spec phase — the placement was verified against the current `Dockerfile` in the discovery draft (Rev 4).

### 8.2 Data storage & privacy

- No user data is collected, stored, or transmitted by this feature; all artifacts are built and published through the existing GitHub Actions + GitHub Releases flow.
- Release packages embed third-party license texts and the attribution manifest (FR-6); checksums and manifests are generated per flavor for auditability.
- The collision audit relies on codepoint data from the pinned v3.5.0 distribution; the pinned distribution and its checksum are part of the build inputs, not runtime data.

### 8.3 Scalability & potential technical challenges

- **Patching runtime**: Up to 7 weights × 2 formats (TTF/OTF) patched per build; patching and regenerated webfonts must fit within the ≤ 300-minute build budget (FR-8.4). Parallelizing patching runs is the obvious lever.
- **Reproducibility**: The patcher source and icon data MUST be pinned to v3.5.0 and checksum-verified (FR-3.2); any nondeterminism in the patcher (timestamps, ordering) must be assessed during the Spec phase against the repository's deterministic-build contract.
- **Format & metadata divergence**: Patching changes metrics, widths, and naming; OTF and webfont outputs must be regenerated from the patcher outputs with independent checksums — stale base-derived files are a defect (FR-5.4).
- **Future icon-set drift**: Newer icon sets may collide with Fantasque's PUA ranges beyond the per-weight AuthorizedOverwriteAllowlist; the collision report is the gate that catches drift (FR-4.3), so it must run per weight per build.
- **Release size**: The Nerd Font Flavor roughly doubles archive surface; release-size growth should be tracked but is accepted as inherent to the feature (the icon set is the point).
- **Visual review burden**: The 427-glyph fallback and the per-weight collision surface (bounded by the AuthorizedOverwriteAllowlist, up to the 75-codepoint PUA union) require Type Designer time; the collision surface is small by design, the fallback backlog is tracked separately in `tracking.json`.

## 9. Milestones & sequencing

### 9.1 Project estimate & Team composition

- **Size**: Medium — 6–8 weeks
- **Team**: Build Engineer (pipeline lead), Type Designer (part-time, visual review + fallback acceptance), Maintainer (release, licensing manifest review)

### 9.2 Suggested phases

- **Phase 1 — Native fallback policy (prerequisite, 2–3 weeks)**: `fallback_regular` status in detection/validation, fallback preparation in the interpolation input, disclosure report fields. The build is unblocked end-to-end before any patching work starts.
- **Phase 2 — Patcher stage (1–2 weeks)**: `builder-nerd-patcher` Docker stage, pinned v3.5.0 + checksum, `--complete` invocation, output namespace isolation, `COPY --from` wiring into Stage 2.
- **Phase 3 — Packaging parity & gates (1–2 weeks)**: Nerd Font packaging branch in `Scripts/packaging.sh`, naming per FR-5.3, independent manifests/checksums, license/attribution manifest, collision report, post-patch validation gates.
- **Phase 4 — Visual review & release (1 week)**: Type Designer review of collision surface, specimen/docs updates, release notes with fallback disclosure, backward-compatibility regression run (SM-T4).

## 10. User stories & Acceptance Criteria

### 10.1. Developer & terminal user installs the Nerd Font Flavor with the complete icon set

- **ID**: GH-001
- **Story**: As a developer and terminal user, I want to download one Nerd Font Flavor archive that contains the complete v3.5.0 icon set on every released weight, so that my terminal, tmux, and IDE icon setups render correctly without manual patching.
- **Acceptance criteria**:
  - [ ] The release provides Nerd Font Flavor archives for TTF and OTF containing every released monospace weight (Regular, Bold, Italic, BoldItalic, Medium, SemiBold, plus stretch weights that pass their gates).
  - [ ] Installing the Nerd Font Flavor and selecting "Fantasque Sans Mono Nerd Font" renders icons from all icon sets enabled by the `--complete` policy at their standard codepoints.
  - [ ] The Nerd Font Flavor family name and file names are distinct from the Base Flavor (FR-5.3), so both can be installed side by side without conflict.
  - [ ] No native Latin, Greek, Cyrillic, digit, or punctuation glyph is missing or altered in the Nerd Font Flavor; overwrites occur only at the per-weight AuthorizedOverwriteAllowlist (FR-4.1).

### 10.2. User is informed about the native-glyph fallback limitation

- **ID**: GH-002
- **Story**: As a user, I want the release notes and README to disclose which glyphs use the Regular outline as a fallback in the new weights, so that I can judge the visual limitation before adopting the font.
- **Acceptance criteria**:
  - [ ] Release notes state that 427 native glyphs render with the **Regular outline in Medium/SemiBold/Light, and with the Bold outline in ExtraBold**, with a pointer to the detailed per-weight fallback report (FR-2.3).
  - [ ] The README documents the fallback, the collision policy, and PUA rendering behavior (FR-2.4, §5.3).
  - [ ] The Visual Quality Rubric explicitly classifies the weight inconsistency as an accepted limitation for release.

### 10.3. Fork Owner receives both flavors from Custom Builds without new configuration

- **ID**: GH-003
- **Story**: As a Fork Owner, I want every Custom Build with multi-weight enabled to include the Nerd Font Flavor automatically, so that I get feature parity with the upstream release without learning new options.
- **Acceptance criteria**:
  - [ ] No new `workflow_dispatch` input is added; multi-weight builds produce both flavors by default (FR-1.3).
  - [ ] Builds with multi-weight disabled produce no Nerd Font artifacts and keep today's artifact set, naming, and structure — no patcher side effects (FR-7.1, SM-T4); manifest `build_timestamp` may differ by design.
  - [ ] The Nerd Font Flavor in Custom Build outputs follows the same naming and packaging conventions as the upstream release (FR-5.3).

### 10.4. Type Designer validates the collision surface before release

- **ID**: GH-004
- **Story**: As a Type Designer, I want a collision report and visual review of the overwritten codepoints, so that I can confirm the Nerd Font Flavor never damages Fantasque glyphs beyond the per-weight AuthorizedOverwriteAllowlist.
- **Acceptance criteria**:
  - [ ] A collision report is generated per weight recording ObservedOverwrite, ExpectedOverwrite, and Added sets, and the drift gate `ObservedOverwrite ⊆ AuthorizedOverwriteAllowlist` holds with zero drift (FR-4.3, SM-T1).
  - [ ] Any overwrite outside the AuthorizedOverwriteAllowlist fails the build gate (FR-4.1).
  - [ ] The overwritten surface (stylistic alternates and native Powerline symbols) passes Type Designer visual review before release (FR-4.4).
  - [ ] The Base Flavor shows no patcher-induced changes in any build mode (FR-4.2, FR-7.2).

### 10.5. Maintainer publishes Nerd Font artifacts with full format parity and license attribution

- **ID**: GH-005
- **Story**: As a Maintainer, I want the Nerd Font Flavor to ship complete formats, independent manifests, and the full icon-set license manifest, so that the release is consistent, auditable, and legally clean.
- **Acceptance criteria**:
  - [ ] Every released Nerd Font weight ships TTF, OTF, WOFF, and WOFF2 with independent manifest and checksums (FR-5.1, FR-5.2, SM-T2).
  - [ ] OTF and webfont outputs are generated from the patcher outputs; no base-derived file ships under Nerd Font names (FR-5.4).
  - [ ] The package includes upstream license texts and an attribution manifest covering 100% of bundled icon sets (FR-6.1, SM-T5).
  - [ ] The Base Flavor package and its contents are unchanged (FR-6.2).

### 10.6. Build Engineer keeps the pipeline reproducible and backward compatible

- **ID**: GH-006
- **Story**: As a Build Engineer, I want the patcher pinned to a checksum-verified v3.5.0 inside a dedicated Docker stage, so that builds are reproducible and the existing workflow structure stays intact.
- **Acceptance criteria**:
  - [ ] The patcher runs in the dedicated `builder-nerd-patcher` stage between Stage 1 and Stage 2, from a pinned v3.5.0 distribution verified by checksum (FR-3.2, FR-8.1).
  - [ ] No new workflow, job, or Actions step is added; `custom-build.yml` keeps its step structure and `test-multi-weight.yml` is untouched (FR-8.3, FR-7.3).
  - [ ] The patcher writes only to its own output namespace; base fonts, `.sfdir` sources, and existing webfont outputs are never written to (FR-9.2).
  - [ ] Total build duration stays within 300 minutes on a free-tier runner (FR-8.4, SM-T3).
  - [ ] Post-patch gates pass for every released weight: metrics, widths, naming, completeness (FR-9.1).
